@prefix usf: <urn:usf:ontology:>.
@prefix port: <urn:usf:port:>.
@prefix iface: <urn:usf:interface:>.
@prefix op: <urn:usf:operation:>.
@prefix sc: <urn:usf:semanticcontract:>.
@prefix perm: <urn:usf:permission:>.
@prefix aud: <urn:usf:auditevent:>.

<urn:usf:graph:interfaces> {
op:auditeventsget a usf:Query;
    usf:canonicalName "auditeventsget";
    usf:requiresPermission perm:auditread.
op:auditeventslist a usf:Query;
    usf:canonicalName "auditeventslist";
    usf:requiresPermission perm:auditsearch.
op:auditverifypost a usf:Query;
    usf:canonicalName "auditverifypost";
    usf:requiresPermission perm:auditverify.
op:authloginpost a usf:Command;
    usf:canonicalName "authloginpost";
    usf:emitsAuditEvent aud:authenticationloginattempt;
    usf:requiresPermission perm:authlogin.
op:apikeycreate a usf:Command;
    usf:canonicalName "apikeycreate";
    usf:emitsAuditEvent aud:apikeycreation;
    usf:requiresPermission perm:apikeycreate.
op:authzcheckpost a usf:Query;
    usf:canonicalName "authzcheckpost";
    usf:requiresPermission perm:authorizationcheck.
op:authzpermissionsget a usf:Query;
    usf:canonicalName "authzpermissionsget";
    usf:requiresPermission perm:tenantmembersread.
op:configcurrentget a usf:Query;
    usf:canonicalName "configcurrentget";
    usf:requiresPermission perm:configread.
op:configflagsget a usf:Query;
    usf:canonicalName "configflagsget";
    usf:requiresPermission perm:configread.
op:configproviderstatusget a usf:Query;
    usf:canonicalName "configproviderstatusget";
    usf:requiresPermission perm:configread.
op:filesdownload a usf:Query;
    usf:canonicalName "filesdownload";
    usf:requiresPermission perm:filedownload.
op:filesget a usf:Query;
    usf:canonicalName "filesget";
    usf:requiresPermission perm:fileread.
op:fileslist a usf:Query;
    usf:canonicalName "fileslist";
    usf:requiresPermission perm:filelist.
op:filesupload a usf:Command;
    usf:canonicalName "filesupload";
    usf:emitsAuditEvent aud:fileupload;
    usf:requiresPermission perm:filecreate.
op:filesverify a usf:Query;
    usf:canonicalName "filesverify";
    usf:requiresPermission perm:fileread.
op:healthzget a usf:Query;
    usf:canonicalName "healthzget";
    usf:requiresPermission perm:routeaccessnone.
op:jobscancel a usf:Command;
    usf:canonicalName "jobscancel";
    usf:emitsAuditEvent aud:jobcancellation;
    usf:requiresPermission perm:jobcancel.
op:jobscreate a usf:Command;
    usf:canonicalName "jobscreate";
    usf:emitsAuditEvent aud:jobcreation;
    usf:requiresPermission perm:jobcreate.
op:jobsget a usf:Query;
    usf:canonicalName "jobsget";
    usf:requiresPermission perm:jobread.
op:jobslist a usf:Query;
    usf:canonicalName "jobslist";
    usf:requiresPermission perm:joblist.
op:notificationscancel a usf:Command;
    usf:canonicalName "notificationscancel";
    usf:emitsAuditEvent aud:notificationcancellation;
    usf:requiresPermission perm:notificationcancel.
op:notificationscreate a usf:Command;
    usf:canonicalName "notificationscreate";
    usf:emitsAuditEvent aud:notificationcreation;
    usf:requiresPermission perm:notificationcreate.
op:notificationsget a usf:Query;
    usf:canonicalName "notificationsget";
    usf:requiresPermission perm:notificationread.
op:notificationslist a usf:Query;
    usf:canonicalName "notificationslist";
    usf:requiresPermission perm:notificationlist.
op:notificationssend a usf:Command;
    usf:canonicalName "notificationssend";
    usf:emitsAuditEvent aud:notificationdispatch;
    usf:requiresPermission perm:notificationsend.
op:profiledetailget a usf:Query;
    usf:canonicalName "profiledetailget";
    usf:requiresPermission perm:tenantmembersread.
op:profilelistget a usf:Query;
    usf:canonicalName "profilelistget";
    usf:requiresPermission perm:tenantmembersread.
op:notificationtemplatescreate a usf:Command;
    usf:canonicalName "notificationtemplatescreate";
    usf:emitsAuditEvent aud:notificationtemplatecreation;
    usf:requiresPermission perm:notificationtemplatecreate.
op:observabilityreadinessget a usf:Query;
    usf:canonicalName "observabilityreadinessget";
    usf:requiresPermission perm:observabilityreadinessread.
op:observabilitysignalslist a usf:Query;
    usf:canonicalName "observabilitysignalslist";
    usf:requiresPermission perm:observabilitysignalread.
op:openapiget a usf:Query;
    usf:canonicalName "openapiget";
    usf:requiresPermission perm:routeaccessnone.
op:providersget a usf:Query;
    usf:canonicalName "providersget";
    usf:requiresPermission perm:providerread.
op:providerslist a usf:Query;
    usf:canonicalName "providerslist";
    usf:requiresPermission perm:providerlist.
op:readyzget a usf:Query;
    usf:canonicalName "readyzget";
    usf:requiresPermission perm:routeaccessnone.
op:tenantcontextget a usf:Query;
    usf:canonicalName "tenantcontextget";
    usf:requiresPermission perm:tenantcontextread.
port:analyticseventstore a usf:Port;
    usf:canonicalName "analyticseventstore".
port:apiruntime a usf:Port;
    usf:canonicalName "apiruntime".
port:auditeventledger a usf:Port;
    usf:canonicalName "auditeventledger".
port:auditexporter a usf:Port;
    usf:canonicalName "auditexporter".
port:auditledger a usf:Port;
    usf:canonicalName "auditledger".
port:auditrecorder a usf:Port;
    usf:canonicalName "auditrecorder".
port:auditsigner a usf:Port;
    usf:canonicalName "auditsigner".
port:backuprestoreprovider a usf:Port;
    usf:canonicalName "backuprestoreprovider".
port:cacheprovider a usf:Port;
    usf:canonicalName "cacheprovider".
port:cloudprovideremulator a usf:Port;
    usf:canonicalName "cloudprovideremulator".
port:configlayerprovider a usf:Port;
    usf:canonicalName "configlayerprovider".
port:durableworkflowport a usf:Port;
    usf:canonicalName "durableworkflowport".
port:eventbus a usf:Port;
    usf:canonicalName "eventbus".
port:externalsecretmanager a usf:Port;
    usf:canonicalName "externalsecretmanager".
port:featureflagsource a usf:Port;
    usf:canonicalName "featureflagsource".
port:filemetadatastore a usf:Port;
    usf:canonicalName "filemetadatastore".
port:guardrailport a usf:Port;
    usf:canonicalName "guardrailport".
port:identitydirectory a usf:Port;
    usf:canonicalName "identitydirectory".
port:identityprovider a usf:Port;
    usf:canonicalName "identityprovider".
port:importexportport a usf:Port;
    usf:canonicalName "importexportport".
port:mailprovider a usf:Port;
    usf:canonicalName "mailprovider".
port:notificationprovider a usf:Port;
    usf:canonicalName "notificationprovider".
port:objectstore a usf:Port;
    usf:canonicalName "objectstore".
port:observabilitysink a usf:Port;
    usf:canonicalName "observabilitysink".
port:operationaljobport a usf:Port;
    usf:canonicalName "operationaljobport".
port:policydecisionpoint a usf:Port;
    usf:canonicalName "policydecisionpoint".
port:qualitygateprovider a usf:Port;
    usf:canonicalName "qualitygateprovider".
port:resourcelifecycleport a usf:Port;
    usf:canonicalName "resourcelifecycleport".
port:scanprovider a usf:Port;
    usf:canonicalName "scanprovider".
port:searchindexport a usf:Port;
    usf:canonicalName "searchindexport".
port:secretresolver a usf:Port;
    usf:canonicalName "secretresolver".
port:secretstore a usf:Port;
    usf:canonicalName "secretstore".
port:sessionstore a usf:Port;
    usf:canonicalName "sessionstore".
port:siemforwarder a usf:Port;
    usf:canonicalName "siemforwarder".
port:signedurlissuer a usf:Port;
    usf:canonicalName "signedurlissuer".
port:telemetryport a usf:Port;
    usf:canonicalName "telemetryport".
port:tenantmembershipdirectory a usf:Port;
    usf:canonicalName "tenantmembershipdirectory".
port:tenantscopedrepository a usf:Port;
    usf:canonicalName "tenantscopedrepository".
port:webhookcaptureprovider a usf:Port;
    usf:canonicalName "webhookcaptureprovider".
port:workflowengine a usf:Port;
    usf:canonicalName "workflowengine".
op:apikeycreate usf:idempotent true;
    usf:idempotencyKeyRequired true;
    usf:timeoutMilliseconds 30000;
    usf:concurrencyBehaviour "Optimistic concurrency with explicit conflict response; no last-write-wins mutation.";
    usf:operationErrorBehaviour "Invalid input, authentication, authorisation, tenant mismatch, conflict, timeout, dependency unavailability and internal failure have distinct stable codes; failures disclose no secret or cross-tenant data and never partially mutate state.".
op:auditeventsget usf:idempotent true;
    usf:idempotencyKeyRequired false;
    usf:timeoutMilliseconds 5000;
    usf:concurrencyBehaviour "Read-only snapshot scoped to the resolved tenant and actor context.";
    usf:operationErrorBehaviour "Invalid input, authentication, authorisation, tenant mismatch, conflict, timeout, dependency unavailability and internal failure have distinct stable codes; failures disclose no secret or cross-tenant data and never partially mutate state.".
op:auditeventslist usf:idempotent true;
    usf:idempotencyKeyRequired false;
    usf:timeoutMilliseconds 5000;
    usf:concurrencyBehaviour "Read-only snapshot scoped to the resolved tenant and actor context.";
    usf:operationErrorBehaviour "Invalid input, authentication, authorisation, tenant mismatch, conflict, timeout, dependency unavailability and internal failure have distinct stable codes; failures disclose no secret or cross-tenant data and never partially mutate state.".
op:auditverifypost usf:idempotent true;
    usf:idempotencyKeyRequired false;
    usf:timeoutMilliseconds 5000;
    usf:concurrencyBehaviour "Read-only snapshot scoped to the resolved tenant and actor context.";
    usf:operationErrorBehaviour "Invalid input, authentication, authorisation, tenant mismatch, conflict, timeout, dependency unavailability and internal failure have distinct stable codes; failures disclose no secret or cross-tenant data and never partially mutate state.".
op:authloginpost usf:idempotent false;
    usf:idempotencyKeyRequired false;
    usf:timeoutMilliseconds 30000;
    usf:concurrencyBehaviour "Optimistic concurrency with explicit conflict response; no last-write-wins mutation.";
    usf:operationErrorBehaviour "Invalid input, authentication, authorisation, tenant mismatch, conflict, timeout, dependency unavailability and internal failure have distinct stable codes; failures disclose no secret or cross-tenant data and never partially mutate state.".
op:authzcheckpost usf:idempotent true;
    usf:idempotencyKeyRequired false;
    usf:timeoutMilliseconds 5000;
    usf:concurrencyBehaviour "Read-only snapshot scoped to the resolved tenant and actor context.";
    usf:operationErrorBehaviour "Invalid input, authentication, authorisation, tenant mismatch, conflict, timeout, dependency unavailability and internal failure have distinct stable codes; failures disclose no secret or cross-tenant data and never partially mutate state.".
op:authzpermissionsget usf:idempotent true;
    usf:idempotencyKeyRequired false;
    usf:timeoutMilliseconds 5000;
    usf:concurrencyBehaviour "Read-only snapshot scoped to the resolved tenant and actor context.";
    usf:operationErrorBehaviour "Invalid input, authentication, authorisation, tenant mismatch, conflict, timeout, dependency unavailability and internal failure have distinct stable codes; failures disclose no secret or cross-tenant data and never partially mutate state.".
op:configcurrentget usf:idempotent true;
    usf:idempotencyKeyRequired false;
    usf:timeoutMilliseconds 5000;
    usf:concurrencyBehaviour "Read-only snapshot scoped to the resolved tenant and actor context.";
    usf:operationErrorBehaviour "Invalid input, authentication, authorisation, tenant mismatch, conflict, timeout, dependency unavailability and internal failure have distinct stable codes; failures disclose no secret or cross-tenant data and never partially mutate state.".
op:configflagsget usf:idempotent true;
    usf:idempotencyKeyRequired false;
    usf:timeoutMilliseconds 5000;
    usf:concurrencyBehaviour "Read-only snapshot scoped to the resolved tenant and actor context.";
    usf:operationErrorBehaviour "Invalid input, authentication, authorisation, tenant mismatch, conflict, timeout, dependency unavailability and internal failure have distinct stable codes; failures disclose no secret or cross-tenant data and never partially mutate state.".
op:configproviderstatusget usf:idempotent true;
    usf:idempotencyKeyRequired false;
    usf:timeoutMilliseconds 5000;
    usf:concurrencyBehaviour "Read-only snapshot scoped to the resolved tenant and actor context.";
    usf:operationErrorBehaviour "Invalid input, authentication, authorisation, tenant mismatch, conflict, timeout, dependency unavailability and internal failure have distinct stable codes; failures disclose no secret or cross-tenant data and never partially mutate state.".
op:filesdownload usf:idempotent true;
    usf:idempotencyKeyRequired false;
    usf:timeoutMilliseconds 5000;
    usf:concurrencyBehaviour "Read-only snapshot scoped to the resolved tenant and actor context.";
    usf:operationErrorBehaviour "Invalid input, authentication, authorisation, tenant mismatch, conflict, timeout, dependency unavailability and internal failure have distinct stable codes; failures disclose no secret or cross-tenant data and never partially mutate state.".
op:filesget usf:idempotent true;
    usf:idempotencyKeyRequired false;
    usf:timeoutMilliseconds 5000;
    usf:concurrencyBehaviour "Read-only snapshot scoped to the resolved tenant and actor context.";
    usf:operationErrorBehaviour "Invalid input, authentication, authorisation, tenant mismatch, conflict, timeout, dependency unavailability and internal failure have distinct stable codes; failures disclose no secret or cross-tenant data and never partially mutate state.".
op:fileslist usf:idempotent true;
    usf:idempotencyKeyRequired false;
    usf:timeoutMilliseconds 5000;
    usf:concurrencyBehaviour "Read-only snapshot scoped to the resolved tenant and actor context.";
    usf:operationErrorBehaviour "Invalid input, authentication, authorisation, tenant mismatch, conflict, timeout, dependency unavailability and internal failure have distinct stable codes; failures disclose no secret or cross-tenant data and never partially mutate state.".
op:filesupload usf:idempotent true;
    usf:idempotencyKeyRequired true;
    usf:timeoutMilliseconds 30000;
    usf:concurrencyBehaviour "Optimistic concurrency with explicit conflict response; no last-write-wins mutation.";
    usf:operationErrorBehaviour "Invalid input, authentication, authorisation, tenant mismatch, conflict, timeout, dependency unavailability and internal failure have distinct stable codes; failures disclose no secret or cross-tenant data and never partially mutate state.".
op:filesverify usf:idempotent true;
    usf:idempotencyKeyRequired false;
    usf:timeoutMilliseconds 5000;
    usf:concurrencyBehaviour "Read-only snapshot scoped to the resolved tenant and actor context.";
    usf:operationErrorBehaviour "Invalid input, authentication, authorisation, tenant mismatch, conflict, timeout, dependency unavailability and internal failure have distinct stable codes; failures disclose no secret or cross-tenant data and never partially mutate state.".
op:healthzget usf:idempotent true;
    usf:idempotencyKeyRequired false;
    usf:timeoutMilliseconds 5000;
    usf:concurrencyBehaviour "Read-only snapshot scoped to the resolved tenant and actor context.";
    usf:operationErrorBehaviour "Invalid input, authentication, authorisation, tenant mismatch, conflict, timeout, dependency unavailability and internal failure have distinct stable codes; failures disclose no secret or cross-tenant data and never partially mutate state.".
op:jobscancel usf:idempotent true;
    usf:idempotencyKeyRequired true;
    usf:timeoutMilliseconds 30000;
    usf:concurrencyBehaviour "Optimistic concurrency with explicit conflict response; no last-write-wins mutation.";
    usf:operationErrorBehaviour "Invalid input, authentication, authorisation, tenant mismatch, conflict, timeout, dependency unavailability and internal failure have distinct stable codes; failures disclose no secret or cross-tenant data and never partially mutate state.".
op:jobscreate usf:idempotent true;
    usf:idempotencyKeyRequired true;
    usf:timeoutMilliseconds 30000;
    usf:concurrencyBehaviour "Optimistic concurrency with explicit conflict response; no last-write-wins mutation.";
    usf:operationErrorBehaviour "Invalid input, authentication, authorisation, tenant mismatch, conflict, timeout, dependency unavailability and internal failure have distinct stable codes; failures disclose no secret or cross-tenant data and never partially mutate state.".
op:jobsget usf:idempotent true;
    usf:idempotencyKeyRequired false;
    usf:timeoutMilliseconds 5000;
    usf:concurrencyBehaviour "Read-only snapshot scoped to the resolved tenant and actor context.";
    usf:operationErrorBehaviour "Invalid input, authentication, authorisation, tenant mismatch, conflict, timeout, dependency unavailability and internal failure have distinct stable codes; failures disclose no secret or cross-tenant data and never partially mutate state.".
op:jobslist usf:idempotent true;
    usf:idempotencyKeyRequired false;
    usf:timeoutMilliseconds 5000;
    usf:concurrencyBehaviour "Read-only snapshot scoped to the resolved tenant and actor context.";
    usf:operationErrorBehaviour "Invalid input, authentication, authorisation, tenant mismatch, conflict, timeout, dependency unavailability and internal failure have distinct stable codes; failures disclose no secret or cross-tenant data and never partially mutate state.".
op:notificationscancel usf:idempotent true;
    usf:idempotencyKeyRequired true;
    usf:timeoutMilliseconds 30000;
    usf:concurrencyBehaviour "Optimistic concurrency with explicit conflict response; no last-write-wins mutation.";
    usf:operationErrorBehaviour "Invalid input, authentication, authorisation, tenant mismatch, conflict, timeout, dependency unavailability and internal failure have distinct stable codes; failures disclose no secret or cross-tenant data and never partially mutate state.".
op:notificationscreate usf:idempotent true;
    usf:idempotencyKeyRequired true;
    usf:timeoutMilliseconds 30000;
    usf:concurrencyBehaviour "Optimistic concurrency with explicit conflict response; no last-write-wins mutation.";
    usf:operationErrorBehaviour "Invalid input, authentication, authorisation, tenant mismatch, conflict, timeout, dependency unavailability and internal failure have distinct stable codes; failures disclose no secret or cross-tenant data and never partially mutate state.".
op:notificationsget usf:idempotent true;
    usf:idempotencyKeyRequired false;
    usf:timeoutMilliseconds 5000;
    usf:concurrencyBehaviour "Read-only snapshot scoped to the resolved tenant and actor context.";
    usf:operationErrorBehaviour "Invalid input, authentication, authorisation, tenant mismatch, conflict, timeout, dependency unavailability and internal failure have distinct stable codes; failures disclose no secret or cross-tenant data and never partially mutate state.".
op:notificationslist usf:idempotent true;
    usf:idempotencyKeyRequired false;
    usf:timeoutMilliseconds 5000;
    usf:concurrencyBehaviour "Read-only snapshot scoped to the resolved tenant and actor context.";
    usf:operationErrorBehaviour "Invalid input, authentication, authorisation, tenant mismatch, conflict, timeout, dependency unavailability and internal failure have distinct stable codes; failures disclose no secret or cross-tenant data and never partially mutate state.".
op:notificationssend usf:idempotent true;
    usf:idempotencyKeyRequired true;
    usf:timeoutMilliseconds 30000;
    usf:concurrencyBehaviour "Optimistic concurrency with explicit conflict response; no last-write-wins mutation.";
    usf:operationErrorBehaviour "Invalid input, authentication, authorisation, tenant mismatch, conflict, timeout, dependency unavailability and internal failure have distinct stable codes; failures disclose no secret or cross-tenant data and never partially mutate state.".
op:notificationtemplatescreate usf:idempotent true;
    usf:idempotencyKeyRequired true;
    usf:timeoutMilliseconds 30000;
    usf:concurrencyBehaviour "Optimistic concurrency with explicit conflict response; no last-write-wins mutation.";
    usf:operationErrorBehaviour "Invalid input, authentication, authorisation, tenant mismatch, conflict, timeout, dependency unavailability and internal failure have distinct stable codes; failures disclose no secret or cross-tenant data and never partially mutate state.".
op:observabilityreadinessget usf:idempotent true;
    usf:idempotencyKeyRequired false;
    usf:timeoutMilliseconds 5000;
    usf:concurrencyBehaviour "Read-only snapshot scoped to the resolved tenant and actor context.";
    usf:operationErrorBehaviour "Invalid input, authentication, authorisation, tenant mismatch, conflict, timeout, dependency unavailability and internal failure have distinct stable codes; failures disclose no secret or cross-tenant data and never partially mutate state.".
op:observabilitysignalslist usf:idempotent true;
    usf:idempotencyKeyRequired false;
    usf:timeoutMilliseconds 5000;
    usf:concurrencyBehaviour "Read-only snapshot scoped to the resolved tenant and actor context.";
    usf:operationErrorBehaviour "Invalid input, authentication, authorisation, tenant mismatch, conflict, timeout, dependency unavailability and internal failure have distinct stable codes; failures disclose no secret or cross-tenant data and never partially mutate state.".
op:openapiget usf:idempotent true;
    usf:idempotencyKeyRequired false;
    usf:timeoutMilliseconds 5000;
    usf:concurrencyBehaviour "Read-only snapshot scoped to the resolved tenant and actor context.";
    usf:operationErrorBehaviour "Invalid input, authentication, authorisation, tenant mismatch, conflict, timeout, dependency unavailability and internal failure have distinct stable codes; failures disclose no secret or cross-tenant data and never partially mutate state.".
op:profiledetailget usf:idempotent true;
    usf:idempotencyKeyRequired false;
    usf:timeoutMilliseconds 5000;
    usf:concurrencyBehaviour "Read-only snapshot scoped to the resolved tenant and actor context.";
    usf:operationErrorBehaviour "Invalid input, authentication, authorisation, tenant mismatch, conflict, timeout, dependency unavailability and internal failure have distinct stable codes; failures disclose no secret or cross-tenant data and never partially mutate state.".
op:profilelistget usf:idempotent true;
    usf:idempotencyKeyRequired false;
    usf:timeoutMilliseconds 5000;
    usf:concurrencyBehaviour "Read-only snapshot scoped to the resolved tenant and actor context.";
    usf:operationErrorBehaviour "Invalid input, authentication, authorisation, tenant mismatch, conflict, timeout, dependency unavailability and internal failure have distinct stable codes; failures disclose no secret or cross-tenant data and never partially mutate state.".
op:providersget usf:idempotent true;
    usf:idempotencyKeyRequired false;
    usf:timeoutMilliseconds 5000;
    usf:concurrencyBehaviour "Read-only snapshot scoped to the resolved tenant and actor context.";
    usf:operationErrorBehaviour "Invalid input, authentication, authorisation, tenant mismatch, conflict, timeout, dependency unavailability and internal failure have distinct stable codes; failures disclose no secret or cross-tenant data and never partially mutate state.".
op:providerslist usf:idempotent true;
    usf:idempotencyKeyRequired false;
    usf:timeoutMilliseconds 5000;
    usf:concurrencyBehaviour "Read-only snapshot scoped to the resolved tenant and actor context.";
    usf:operationErrorBehaviour "Invalid input, authentication, authorisation, tenant mismatch, conflict, timeout, dependency unavailability and internal failure have distinct stable codes; failures disclose no secret or cross-tenant data and never partially mutate state.".
op:readyzget usf:idempotent true;
    usf:idempotencyKeyRequired false;
    usf:timeoutMilliseconds 5000;
    usf:concurrencyBehaviour "Read-only snapshot scoped to the resolved tenant and actor context.";
    usf:operationErrorBehaviour "Invalid input, authentication, authorisation, tenant mismatch, conflict, timeout, dependency unavailability and internal failure have distinct stable codes; failures disclose no secret or cross-tenant data and never partially mutate state.".
op:tenantcontextget usf:idempotent true;
    usf:idempotencyKeyRequired false;
    usf:timeoutMilliseconds 5000;
    usf:concurrencyBehaviour "Read-only snapshot scoped to the resolved tenant and actor context.";
    usf:operationErrorBehaviour "Invalid input, authentication, authorisation, tenant mismatch, conflict, timeout, dependency unavailability and internal failure have distinct stable codes; failures disclose no secret or cross-tenant data and never partially mutate state.".
iface:abacpolicydecisionpointoperations a usf:Interface;
    usf:canonicalName "abacpolicydecisionpointoperations";
    usf:repositoryName "attribute-based-access-control-policy-decision-point-operations";
    usf:definition "Callable command and query surface owned by attribute based access control policy decision point.";
    usf:interfaceForContract sc:abacpolicydecisionpoint;
    usf:hasOperation op:authzcheckpost.
iface:apikeyspersonalaccesstokensoperations a usf:Interface;
    usf:canonicalName "apikeyspersonalaccesstokensoperations";
    usf:repositoryName "api-keys-personal-access-tokens-operations";
    usf:definition "Callable command and query surface owned by api keys personal access tokens.";
    usf:interfaceForContract sc:apikeyspersonalaccesstokens;
    usf:hasOperation op:apikeycreate.
iface:auditofprivilegedaccessoperations a usf:Interface;
    usf:canonicalName "auditofprivilegedaccessoperations";
    usf:repositoryName "audit-of-privileged-access-operations";
    usf:definition "Callable command and query surface owned by audit of privileged access.";
    usf:interfaceForContract sc:auditofprivilegedaccess;
    usf:hasOperation op:auditeventsget, op:auditeventslist, op:auditverifypost.
iface:authenticationplatformoperations a usf:Interface;
    usf:canonicalName "authenticationplatformoperations";
    usf:repositoryName "authentication-platform-operations";
    usf:definition "Callable command and query surface owned by authentication platform.";
    usf:interfaceForContract sc:authenticationplatform;
    usf:hasOperation op:authloginpost.
iface:configurationregistryandhistoryoperations a usf:Interface;
    usf:canonicalName "configurationregistryandhistoryoperations";
    usf:repositoryName "configuration-registry-history-operations";
    usf:definition "Callable command and query surface owned by configuration registry history.";
    usf:interfaceForContract sc:configurationregistryandhistory;
    usf:hasOperation op:configcurrentget, op:configflagsget.
iface:enduserprofileandpreferencesselfserviceoperations a usf:Interface;
    usf:canonicalName "enduserprofileandpreferencesselfserviceoperations";
    usf:repositoryName "end-user-profile-preferences-self-service-operations";
    usf:definition "Callable command and query surface owned by end user profile preferences self service.";
    usf:interfaceForContract sc:enduserprofileandpreferencesselfservice;
    usf:hasOperation op:profiledetailget, op:profilelistget.
iface:internalservicecatalogandreadinessoperations a usf:Interface;
    usf:canonicalName "internalservicecatalogandreadinessoperations";
    usf:repositoryName "internal-service-catalog-readiness-operations";
    usf:definition "Callable command and query surface owned by internal service catalog readiness.";
    usf:interfaceForContract sc:internalservicecatalogandreadiness;
    usf:hasOperation op:healthzget, op:readyzget.
iface:metricsandtracesoperations a usf:Interface;
    usf:canonicalName "metricsandtracesoperations";
    usf:repositoryName "metrics-traces-operations";
    usf:definition "Callable command and query surface owned by metrics traces.";
    usf:interfaceForContract sc:metricsandtraces;
    usf:hasOperation op:observabilityreadinessget, op:observabilitysignalslist.
iface:notificationdeliveryandpreferencesandchannelsoperations a usf:Interface;
    usf:canonicalName "notificationdeliveryandpreferencesandchannelsoperations";
    usf:repositoryName "notification-delivery-preferences-channels-operations";
    usf:definition "Callable command and query surface owned by notification delivery preferences channels.";
    usf:interfaceForContract sc:notificationdeliveryandpreferencesandchannels;
    usf:hasOperation op:notificationscancel, op:notificationscreate, op:notificationsget, op:notificationslist, op:notificationssend, op:notificationtemplatescreate.
iface:objectstorageandtenantprefixesandsignedurlsoperations a usf:Interface;
    usf:canonicalName "objectstorageandtenantprefixesandsignedurlsoperations";
    usf:repositoryName "object-storage-tenant-prefixes-signed-urls-operations";
    usf:definition "Callable command and query surface owned by object storage tenant prefixes signed urls.";
    usf:interfaceForContract sc:objectstorageandtenantprefixesandsignedurls;
    usf:hasOperation op:filesdownload, op:filesget, op:fileslist, op:filesupload, op:filesverify.
iface:openapidrifthardgateoperations a usf:Interface;
    usf:canonicalName "openapidrifthardgateoperations";
    usf:repositoryName "open-api-drift-gate-operations";
    usf:definition "Callable command and query surface owned by open api drift gate.";
    usf:interfaceForContract sc:openapidrifthardgate;
    usf:hasOperation op:openapiget.
iface:providerconfigurationplaneoperations a usf:Interface;
    usf:canonicalName "providerconfigurationplaneoperations";
    usf:repositoryName "provider-configuration-plane-operations";
    usf:definition "Callable command and query surface owned by provider configuration plane.";
    usf:interfaceForContract sc:providerconfigurationplane;
    usf:hasOperation op:providersget, op:providerslist.
iface:providerenvironmentclassificationoperations a usf:Interface;
    usf:canonicalName "providerenvironmentclassificationoperations";
    usf:repositoryName "provider-environment-classification-operations";
    usf:definition "Callable command and query surface owned by provider environment classification.";
    usf:interfaceForContract sc:providerenvironmentclassification;
    usf:hasOperation op:configproviderstatusget.
iface:rbacrolesandpermissionsoperations a usf:Interface;
    usf:canonicalName "rbacrolesandpermissionsoperations";
    usf:repositoryName "role-based-access-control-roles-permissions-operations";
    usf:definition "Callable command and query surface owned by role based access control roles permissions.";
    usf:interfaceForContract sc:rbacrolesandpermissions;
    usf:hasOperation op:authzpermissionsget.
iface:scheduledjobsbuiltinontheeventsubstrateoperations a usf:Interface;
    usf:canonicalName "scheduledjobsbuiltinontheeventsubstrateoperations";
    usf:repositoryName "scheduled-jobs-event-substrate-operations";
    usf:definition "Callable command and query surface owned by scheduled jobs event substrate.";
    usf:interfaceForContract sc:scheduledjobsbuiltinontheeventsubstrate;
    usf:hasOperation op:jobscancel, op:jobscreate, op:jobsget, op:jobslist.
iface:tenanthostidentityresolutionoperations a usf:Interface;
    usf:canonicalName "tenanthostidentityresolutionoperations";
    usf:repositoryName "tenant-host-identity-resolution-operations";
    usf:definition "Callable command and query surface owned by tenant host identity resolution.";
    usf:interfaceForContract sc:tenanthostidentityresolution;
    usf:hasOperation op:tenantcontextget.
port:analyticseventstore usf:repositoryName "analytics-event-store";
    usf:definition "Provider-independent boundary for analytics event store, owned by usage metering meter event ingestion.";
    usf:portForContract sc:usagemeteringandmetereventingestion;
    usf:permitsProviderMode <urn:usf:providermode:deterministictestsubstitute>, <urn:usf:providermode:repositorylocalservice>, <urn:usf:providermode:externalsandbox>, <urn:usf:providermode:liveexternal>;
    usf:enforcesTenantBoundary <urn:usf:tenantboundary:tenant>;
    usf:providerMaximumAttempts 3;
    usf:providerFailureBehaviour "Validate requests and responses, apply a bounded timeout, retry only classified transient failures, preserve idempotency, open a bounded circuit after repeated failure, and return a stable redacted dependency error.";
    usf:providerSecurityBoundary "Credentials are secret references scoped to this provider and environment; tenant context is explicit, outbound destinations are allow-listed, and request and response audit metadata exclude payload secrets.".
port:apiruntime usf:repositoryName "api-runtime";
    usf:definition "Provider-independent boundary for api runtime, owned by universal service foundation scope principles.";
    usf:portForContract sc:universalservicefoundationscopeandprinciples;
    usf:permitsProviderMode <urn:usf:providermode:deterministictestsubstitute>, <urn:usf:providermode:repositorylocalservice>;
    usf:enforcesTenantBoundary <urn:usf:tenantboundary:tenant>;
    usf:providerMaximumAttempts 3;
    usf:providerFailureBehaviour "Validate requests and responses, apply a bounded timeout, retry only classified transient failures, preserve idempotency, open a bounded circuit after repeated failure, and return a stable redacted dependency error.";
    usf:providerSecurityBoundary "Credentials are secret references scoped to this provider and environment; tenant context is explicit, outbound destinations are allow-listed, and request and response audit metadata exclude payload secrets.".
port:auditeventledger usf:repositoryName "audit-event-ledger";
    usf:definition "Provider-independent boundary for audit event ledger, owned by audit of privileged access.";
    usf:portForContract sc:auditofprivilegedaccess;
    usf:permitsProviderMode <urn:usf:providermode:deterministictestsubstitute>, <urn:usf:providermode:repositorylocalservice>;
    usf:enforcesTenantBoundary <urn:usf:tenantboundary:tenant>;
    usf:providerMaximumAttempts 3;
    usf:providerFailureBehaviour "Validate requests and responses, apply a bounded timeout, retry only classified transient failures, preserve idempotency, open a bounded circuit after repeated failure, and return a stable redacted dependency error.";
    usf:providerSecurityBoundary "Credentials are secret references scoped to this provider and environment; tenant context is explicit, outbound destinations are allow-listed, and request and response audit metadata exclude payload secrets.".
port:auditexporter usf:repositoryName "audit-exporter";
    usf:definition "Provider-independent boundary for audit exporter, owned by audit of privileged access.";
    usf:portForContract sc:auditofprivilegedaccess;
    usf:permitsProviderMode <urn:usf:providermode:deterministictestsubstitute>, <urn:usf:providermode:repositorylocalservice>, <urn:usf:providermode:externalsandbox>, <urn:usf:providermode:liveexternal>;
    usf:enforcesTenantBoundary <urn:usf:tenantboundary:platform>;
    usf:providerMaximumAttempts 3;
    usf:providerFailureBehaviour "Validate requests and responses, apply a bounded timeout, retry only classified transient failures, preserve idempotency, open a bounded circuit after repeated failure, and return a stable redacted dependency error.";
    usf:providerSecurityBoundary "Credentials are secret references scoped to this provider and environment; tenant context is explicit, outbound destinations are allow-listed, and request and response audit metadata exclude payload secrets.".
port:auditledger usf:repositoryName "audit-ledger";
    usf:definition "Provider-independent boundary for audit ledger, owned by audit of privileged access.";
    usf:portForContract sc:auditofprivilegedaccess;
    usf:permitsProviderMode <urn:usf:providermode:deterministictestsubstitute>, <urn:usf:providermode:repositorylocalservice>;
    usf:enforcesTenantBoundary <urn:usf:tenantboundary:tenant>;
    usf:providerMaximumAttempts 3;
    usf:providerFailureBehaviour "Validate requests and responses, apply a bounded timeout, retry only classified transient failures, preserve idempotency, open a bounded circuit after repeated failure, and return a stable redacted dependency error.";
    usf:providerSecurityBoundary "Credentials are secret references scoped to this provider and environment; tenant context is explicit, outbound destinations are allow-listed, and request and response audit metadata exclude payload secrets.".
port:auditrecorder usf:repositoryName "audit-recorder";
    usf:definition "Provider-independent boundary for audit recorder, owned by audit of privileged access.";
    usf:portForContract sc:auditofprivilegedaccess;
    usf:permitsProviderMode <urn:usf:providermode:deterministictestsubstitute>, <urn:usf:providermode:repositorylocalservice>;
    usf:enforcesTenantBoundary <urn:usf:tenantboundary:tenant>;
    usf:providerMaximumAttempts 3;
    usf:providerFailureBehaviour "Validate requests and responses, apply a bounded timeout, retry only classified transient failures, preserve idempotency, open a bounded circuit after repeated failure, and return a stable redacted dependency error.";
    usf:providerSecurityBoundary "Credentials are secret references scoped to this provider and environment; tenant context is explicit, outbound destinations are allow-listed, and request and response audit metadata exclude payload secrets.".
port:auditsigner usf:repositoryName "audit-signer";
    usf:definition "Provider-independent boundary for audit signer, owned by audit of privileged access.";
    usf:portForContract sc:auditofprivilegedaccess;
    usf:permitsProviderMode <urn:usf:providermode:deterministictestsubstitute>, <urn:usf:providermode:repositorylocalservice>;
    usf:enforcesTenantBoundary <urn:usf:tenantboundary:platform>;
    usf:providerMaximumAttempts 3;
    usf:providerFailureBehaviour "Validate requests and responses, apply a bounded timeout, retry only classified transient failures, preserve idempotency, open a bounded circuit after repeated failure, and return a stable redacted dependency error.";
    usf:providerSecurityBoundary "Credentials are secret references scoped to this provider and environment; tenant context is explicit, outbound destinations are allow-listed, and request and response audit metadata exclude payload secrets.".
port:backuprestoreprovider usf:repositoryName "backup-restore-provider";
    usf:definition "Provider-independent boundary for backup restore provider, owned by backup and restore.";
    usf:portForContract sc:backupandrestore;
    usf:permitsProviderMode <urn:usf:providermode:deterministictestsubstitute>, <urn:usf:providermode:repositorylocalservice>, <urn:usf:providermode:externalsandbox>, <urn:usf:providermode:liveexternal>;
    usf:enforcesTenantBoundary <urn:usf:tenantboundary:platform>;
    usf:providerMaximumAttempts 3;
    usf:providerFailureBehaviour "Validate requests and responses, apply a bounded timeout, retry only classified transient failures, preserve idempotency, open a bounded circuit after repeated failure, and return a stable redacted dependency error.";
    usf:providerSecurityBoundary "Credentials are secret references scoped to this provider and environment; tenant context is explicit, outbound destinations are allow-listed, and request and response audit metadata exclude payload secrets.".
port:cacheprovider usf:repositoryName "cache-provider";
    usf:definition "Provider-independent boundary for cache provider, owned by service catalog provider integration model.";
    usf:portForContract sc:servicecatalogandproviderintegrationmodel;
    usf:permitsProviderMode <urn:usf:providermode:deterministictestsubstitute>, <urn:usf:providermode:repositorylocalservice>;
    usf:enforcesTenantBoundary <urn:usf:tenantboundary:tenant>;
    usf:providerMaximumAttempts 3;
    usf:providerFailureBehaviour "Validate requests and responses, apply a bounded timeout, retry only classified transient failures, preserve idempotency, open a bounded circuit after repeated failure, and return a stable redacted dependency error.";
    usf:providerSecurityBoundary "Credentials are secret references scoped to this provider and environment; tenant context is explicit, outbound destinations are allow-listed, and request and response audit metadata exclude payload secrets.".
port:cloudprovideremulator usf:repositoryName "cloud-provider-emulator";
    usf:definition "Provider-independent boundary for cloud provider emulator, owned by deterministic development test substitutes.";
    usf:portForContract sc:deterministicdevelopmentandtestsubstitutes;
    usf:permitsProviderMode <urn:usf:providermode:deterministictestsubstitute>, <urn:usf:providermode:repositorylocalservice>;
    usf:enforcesTenantBoundary <urn:usf:tenantboundary:platform>;
    usf:providerMaximumAttempts 3;
    usf:providerFailureBehaviour "Validate requests and responses, apply a bounded timeout, retry only classified transient failures, preserve idempotency, open a bounded circuit after repeated failure, and return a stable redacted dependency error.";
    usf:providerSecurityBoundary "Credentials are secret references scoped to this provider and environment; tenant context is explicit, outbound destinations are allow-listed, and request and response audit metadata exclude payload secrets.".
port:configlayerprovider usf:repositoryName "configuration-layer-provider";
    usf:definition "Provider-independent boundary for configuration layer provider, owned by configuration registry history.";
    usf:portForContract sc:configurationregistryandhistory;
    usf:permitsProviderMode <urn:usf:providermode:deterministictestsubstitute>, <urn:usf:providermode:repositorylocalservice>;
    usf:enforcesTenantBoundary <urn:usf:tenantboundary:tenant>;
    usf:providerMaximumAttempts 3;
    usf:providerFailureBehaviour "Validate requests and responses, apply a bounded timeout, retry only classified transient failures, preserve idempotency, open a bounded circuit after repeated failure, and return a stable redacted dependency error.";
    usf:providerSecurityBoundary "Credentials are secret references scoped to this provider and environment; tenant context is explicit, outbound destinations are allow-listed, and request and response audit metadata exclude payload secrets.".
port:durableworkflowport usf:repositoryName "durable-workflow";
    usf:definition "Provider-independent boundary for durable workflow, owned by workflow engine scheduled jobs approvals.";
    usf:portForContract sc:workflowenginescheduledjobsapprovals;
    usf:permitsProviderMode <urn:usf:providermode:deterministictestsubstitute>, <urn:usf:providermode:repositorylocalservice>;
    usf:enforcesTenantBoundary <urn:usf:tenantboundary:tenant>;
    usf:providerMaximumAttempts 3;
    usf:providerFailureBehaviour "Validate requests and responses, apply a bounded timeout, retry only classified transient failures, preserve idempotency, open a bounded circuit after repeated failure, and return a stable redacted dependency error.";
    usf:providerSecurityBoundary "Credentials are secret references scoped to this provider and environment; tenant context is explicit, outbound destinations are allow-listed, and request and response audit metadata exclude payload secrets.".
port:eventbus usf:repositoryName "event-bus";
    usf:definition "Provider-independent boundary for event bus, owned by event bus durable queues dead letter redrive.";
    usf:portForContract sc:eventbusdurablequeuesdlqredrive;
    usf:permitsProviderMode <urn:usf:providermode:deterministictestsubstitute>, <urn:usf:providermode:repositorylocalservice>;
    usf:enforcesTenantBoundary <urn:usf:tenantboundary:tenant>;
    usf:providerMaximumAttempts 3;
    usf:providerFailureBehaviour "Validate requests and responses, apply a bounded timeout, retry only classified transient failures, preserve idempotency, open a bounded circuit after repeated failure, and return a stable redacted dependency error.";
    usf:providerSecurityBoundary "Credentials are secret references scoped to this provider and environment; tenant context is explicit, outbound destinations are allow-listed, and request and response audit metadata exclude payload secrets.".
port:externalsecretmanager usf:repositoryName "external-secret-manager";
    usf:definition "Provider-independent boundary for external secret manager, owned by runtime secrets management.";
    usf:portForContract sc:runtimesecretsmanagement;
    usf:permitsProviderMode <urn:usf:providermode:deterministictestsubstitute>, <urn:usf:providermode:repositorylocalservice>, <urn:usf:providermode:externalsandbox>, <urn:usf:providermode:liveexternal>;
    usf:enforcesTenantBoundary <urn:usf:tenantboundary:platform>;
    usf:providerMaximumAttempts 3;
    usf:providerFailureBehaviour "Validate requests and responses, apply a bounded timeout, retry only classified transient failures, preserve idempotency, open a bounded circuit after repeated failure, and return a stable redacted dependency error.";
    usf:providerSecurityBoundary "Credentials are secret references scoped to this provider and environment; tenant context is explicit, outbound destinations are allow-listed, and request and response audit metadata exclude payload secrets.".
port:featureflagsource usf:repositoryName "feature-flag-source";
    usf:definition "Provider-independent boundary for feature flag source, owned by configuration registry history.";
    usf:portForContract sc:configurationregistryandhistory;
    usf:permitsProviderMode <urn:usf:providermode:deterministictestsubstitute>, <urn:usf:providermode:repositorylocalservice>;
    usf:enforcesTenantBoundary <urn:usf:tenantboundary:tenant>;
    usf:providerMaximumAttempts 3;
    usf:providerFailureBehaviour "Validate requests and responses, apply a bounded timeout, retry only classified transient failures, preserve idempotency, open a bounded circuit after repeated failure, and return a stable redacted dependency error.";
    usf:providerSecurityBoundary "Credentials are secret references scoped to this provider and environment; tenant context is explicit, outbound destinations are allow-listed, and request and response audit metadata exclude payload secrets.".
port:filemetadatastore usf:repositoryName "file-metadata-store";
    usf:definition "Provider-independent boundary for file metadata store, owned by object storage tenant prefixes signed urls.";
    usf:portForContract sc:objectstorageandtenantprefixesandsignedurls;
    usf:permitsProviderMode <urn:usf:providermode:deterministictestsubstitute>, <urn:usf:providermode:repositorylocalservice>;
    usf:enforcesTenantBoundary <urn:usf:tenantboundary:tenant>;
    usf:providerMaximumAttempts 3;
    usf:providerFailureBehaviour "Validate requests and responses, apply a bounded timeout, retry only classified transient failures, preserve idempotency, open a bounded circuit after repeated failure, and return a stable redacted dependency error.";
    usf:providerSecurityBoundary "Credentials are secret references scoped to this provider and environment; tenant context is explicit, outbound destinations are allow-listed, and request and response audit metadata exclude payload secrets.".
port:guardrailport usf:repositoryName "guardrail";
    usf:definition "Provider-independent boundary for guardrail, owned by code quality secret dependency scanning.";
    usf:portForContract sc:codequalityandsecretanddependencyscanning;
    usf:permitsProviderMode <urn:usf:providermode:deterministictestsubstitute>, <urn:usf:providermode:repositorylocalservice>;
    usf:enforcesTenantBoundary <urn:usf:tenantboundary:platform>;
    usf:providerMaximumAttempts 3;
    usf:providerFailureBehaviour "Validate requests and responses, apply a bounded timeout, retry only classified transient failures, preserve idempotency, open a bounded circuit after repeated failure, and return a stable redacted dependency error.";
    usf:providerSecurityBoundary "Credentials are secret references scoped to this provider and environment; tenant context is explicit, outbound destinations are allow-listed, and request and response audit metadata exclude payload secrets.".
port:identitydirectory usf:repositoryName "identity-directory";
    usf:definition "Provider-independent boundary for identity directory, owned by user identity tenant membership.";
    usf:portForContract sc:useridentityandtenantmembership;
    usf:permitsProviderMode <urn:usf:providermode:deterministictestsubstitute>, <urn:usf:providermode:repositorylocalservice>;
    usf:enforcesTenantBoundary <urn:usf:tenantboundary:tenant>;
    usf:providerMaximumAttempts 3;
    usf:providerFailureBehaviour "Validate requests and responses, apply a bounded timeout, retry only classified transient failures, preserve idempotency, open a bounded circuit after repeated failure, and return a stable redacted dependency error.";
    usf:providerSecurityBoundary "Credentials are secret references scoped to this provider and environment; tenant context is explicit, outbound destinations are allow-listed, and request and response audit metadata exclude payload secrets.".
port:identityprovider usf:repositoryName "identity-provider";
    usf:definition "Provider-independent boundary for identity provider, owned by authentication platform.";
    usf:portForContract sc:authenticationplatform;
    usf:permitsProviderMode <urn:usf:providermode:deterministictestsubstitute>, <urn:usf:providermode:repositorylocalservice>, <urn:usf:providermode:externalsandbox>, <urn:usf:providermode:liveexternal>;
    usf:enforcesTenantBoundary <urn:usf:tenantboundary:tenant>;
    usf:providerMaximumAttempts 3;
    usf:providerFailureBehaviour "Retry only transient discovery, signing-key retrieval or provider-network failures with bounded backoff. Invalid signature, issuer, audience, nonce, state, expiry or other credential failures are non-retryable and fail closed. No failed verification creates a session; errors are stable and redact tokens and provider detail.";
    usf:providerSecurityBoundary "Provider configuration and credentials are tenant- and environment-scoped; issuer, audience, signature, expiry, not-before, nonce, state and proof-key exchange are validated; endpoints are allow-listed and transport encryption is mandatory except for explicitly authorised local substitutes; raw tokens, secrets and cross-tenant material never enter logs or responses.".
port:importexportport usf:repositoryName "import-export";
    usf:definition "Provider-independent boundary for import export, owned by tenant data import export.";
    usf:portForContract sc:tenantdataimportexport;
    usf:permitsProviderMode <urn:usf:providermode:deterministictestsubstitute>, <urn:usf:providermode:repositorylocalservice>;
    usf:enforcesTenantBoundary <urn:usf:tenantboundary:tenant>;
    usf:providerMaximumAttempts 3;
    usf:providerFailureBehaviour "Validate requests and responses, apply a bounded timeout, retry only classified transient failures, preserve idempotency, open a bounded circuit after repeated failure, and return a stable redacted dependency error.";
    usf:providerSecurityBoundary "Credentials are secret references scoped to this provider and environment; tenant context is explicit, outbound destinations are allow-listed, and request and response audit metadata exclude payload secrets.".
port:mailprovider usf:repositoryName "mail-provider";
    usf:definition "Provider-independent boundary for mail provider, owned by notification delivery preferences channels.";
    usf:portForContract sc:notificationdeliveryandpreferencesandchannels;
    usf:permitsProviderMode <urn:usf:providermode:deterministictestsubstitute>, <urn:usf:providermode:repositorylocalservice>, <urn:usf:providermode:externalsandbox>, <urn:usf:providermode:liveexternal>;
    usf:enforcesTenantBoundary <urn:usf:tenantboundary:tenant>;
    usf:providerMaximumAttempts 3;
    usf:providerFailureBehaviour "Validate requests and responses, apply a bounded timeout, retry only classified transient failures, preserve idempotency, open a bounded circuit after repeated failure, and return a stable redacted dependency error.";
    usf:providerSecurityBoundary "Credentials are secret references scoped to this provider and environment; tenant context is explicit, outbound destinations are allow-listed, and request and response audit metadata exclude payload secrets.".
port:notificationprovider usf:repositoryName "notification-provider";
    usf:definition "Provider-independent boundary for notification provider, owned by notification delivery preferences channels.";
    usf:portForContract sc:notificationdeliveryandpreferencesandchannels;
    usf:permitsProviderMode <urn:usf:providermode:deterministictestsubstitute>, <urn:usf:providermode:repositorylocalservice>, <urn:usf:providermode:externalsandbox>, <urn:usf:providermode:liveexternal>;
    usf:enforcesTenantBoundary <urn:usf:tenantboundary:tenant>;
    usf:providerMaximumAttempts 3;
    usf:providerFailureBehaviour "Validate requests and responses, apply a bounded timeout, retry only classified transient failures, preserve idempotency, open a bounded circuit after repeated failure, and return a stable redacted dependency error.";
    usf:providerSecurityBoundary "Credentials are secret references scoped to this provider and environment; tenant context is explicit, outbound destinations are allow-listed, and request and response audit metadata exclude payload secrets.".
port:objectstore usf:repositoryName "object-store";
    usf:definition "Provider-independent boundary for object store, owned by object storage tenant prefixes signed urls.";
    usf:portForContract sc:objectstorageandtenantprefixesandsignedurls;
    usf:permitsProviderMode <urn:usf:providermode:deterministictestsubstitute>, <urn:usf:providermode:repositorylocalservice>, <urn:usf:providermode:externalsandbox>, <urn:usf:providermode:liveexternal>;
    usf:enforcesTenantBoundary <urn:usf:tenantboundary:tenant>;
    usf:providerMaximumAttempts 3;
    usf:providerFailureBehaviour "Validate requests and responses, apply a bounded timeout, retry only classified transient failures, preserve idempotency, open a bounded circuit after repeated failure, and return a stable redacted dependency error.";
    usf:providerSecurityBoundary "Credentials are secret references scoped to this provider and environment; tenant context is explicit, outbound destinations are allow-listed, and request and response audit metadata exclude payload secrets.".
port:observabilitysink usf:repositoryName "observability-sink";
    usf:definition "Provider-independent boundary for observability sink, owned by metrics traces.";
    usf:portForContract sc:metricsandtraces;
    usf:permitsProviderMode <urn:usf:providermode:deterministictestsubstitute>, <urn:usf:providermode:repositorylocalservice>, <urn:usf:providermode:externalsandbox>, <urn:usf:providermode:liveexternal>;
    usf:enforcesTenantBoundary <urn:usf:tenantboundary:tenant>;
    usf:providerMaximumAttempts 3;
    usf:providerFailureBehaviour "Validate requests and responses, apply a bounded timeout, retry only classified transient failures, preserve idempotency, open a bounded circuit after repeated failure, and return a stable redacted dependency error.";
    usf:providerSecurityBoundary "Credentials are secret references scoped to this provider and environment; tenant context is explicit, outbound destinations are allow-listed, and request and response audit metadata exclude payload secrets.".
port:operationaljobport usf:repositoryName "operational-job";
    usf:definition "Provider-independent boundary for operational job, owned by background workers job runner.";
    usf:portForContract sc:backgroundworkersjobrunner;
    usf:permitsProviderMode <urn:usf:providermode:deterministictestsubstitute>, <urn:usf:providermode:repositorylocalservice>;
    usf:enforcesTenantBoundary <urn:usf:tenantboundary:tenant>;
    usf:providerMaximumAttempts 3;
    usf:providerFailureBehaviour "Validate requests and responses, apply a bounded timeout, retry only classified transient failures, preserve idempotency, open a bounded circuit after repeated failure, and return a stable redacted dependency error.";
    usf:providerSecurityBoundary "Credentials are secret references scoped to this provider and environment; tenant context is explicit, outbound destinations are allow-listed, and request and response audit metadata exclude payload secrets.".
port:policydecisionpoint usf:repositoryName "policy-decision-point";
    usf:definition "Provider-independent boundary for policy decision point, owned by attribute based access control policy decision point.";
    usf:portForContract sc:abacpolicydecisionpoint;
    usf:permitsProviderMode <urn:usf:providermode:deterministictestsubstitute>, <urn:usf:providermode:repositorylocalservice>;
    usf:enforcesTenantBoundary <urn:usf:tenantboundary:tenant>;
    usf:providerMaximumAttempts 3;
    usf:providerFailureBehaviour "Validate requests and responses, apply a bounded timeout, retry only classified transient failures, preserve idempotency, open a bounded circuit after repeated failure, and return a stable redacted dependency error.";
    usf:providerSecurityBoundary "Credentials are secret references scoped to this provider and environment; tenant context is explicit, outbound destinations are allow-listed, and request and response audit metadata exclude payload secrets.".
port:qualitygateprovider usf:repositoryName "quality-gate-provider";
    usf:definition "Provider-independent boundary for quality gate provider, owned by code quality secret dependency scanning.";
    usf:portForContract sc:codequalityandsecretanddependencyscanning;
    usf:permitsProviderMode <urn:usf:providermode:deterministictestsubstitute>, <urn:usf:providermode:repositorylocalservice>, <urn:usf:providermode:externalsandbox>, <urn:usf:providermode:liveexternal>;
    usf:enforcesTenantBoundary <urn:usf:tenantboundary:platform>;
    usf:providerMaximumAttempts 3;
    usf:providerFailureBehaviour "Validate requests and responses, apply a bounded timeout, retry only classified transient failures, preserve idempotency, open a bounded circuit after repeated failure, and return a stable redacted dependency error.";
    usf:providerSecurityBoundary "Credentials are secret references scoped to this provider and environment; tenant context is explicit, outbound destinations are allow-listed, and request and response audit metadata exclude payload secrets.".
port:resourcelifecycleport usf:repositoryName "resource-lifecycle";
    usf:definition "Provider-independent boundary for resource lifecycle, owned by tenant lifecycle provision suspend delete export.";
    usf:portForContract sc:tenantlifecycleprovisionsuspenddeleteexport;
    usf:permitsProviderMode <urn:usf:providermode:deterministictestsubstitute>, <urn:usf:providermode:repositorylocalservice>;
    usf:enforcesTenantBoundary <urn:usf:tenantboundary:tenant>;
    usf:providerMaximumAttempts 3;
    usf:providerFailureBehaviour "Validate requests and responses, apply a bounded timeout, retry only classified transient failures, preserve idempotency, open a bounded circuit after repeated failure, and return a stable redacted dependency error.";
    usf:providerSecurityBoundary "Credentials are secret references scoped to this provider and environment; tenant context is explicit, outbound destinations are allow-listed, and request and response audit metadata exclude payload secrets.".
port:scanprovider usf:repositoryName "scan-provider";
    usf:definition "Provider-independent boundary for scan provider, owned by code quality secret dependency scanning.";
    usf:portForContract sc:codequalityandsecretanddependencyscanning;
    usf:permitsProviderMode <urn:usf:providermode:deterministictestsubstitute>, <urn:usf:providermode:repositorylocalservice>, <urn:usf:providermode:externalsandbox>, <urn:usf:providermode:liveexternal>;
    usf:enforcesTenantBoundary <urn:usf:tenantboundary:platform>;
    usf:providerMaximumAttempts 3;
    usf:providerFailureBehaviour "Validate requests and responses, apply a bounded timeout, retry only classified transient failures, preserve idempotency, open a bounded circuit after repeated failure, and return a stable redacted dependency error.";
    usf:providerSecurityBoundary "Credentials are secret references scoped to this provider and environment; tenant context is explicit, outbound destinations are allow-listed, and request and response audit metadata exclude payload secrets.".
port:searchindexport usf:repositoryName "search-index";
    usf:definition "Provider-independent boundary for search index, owned by search indexing product search.";
    usf:portForContract sc:searchandindexingproductsearch;
    usf:permitsProviderMode <urn:usf:providermode:deterministictestsubstitute>, <urn:usf:providermode:repositorylocalservice>, <urn:usf:providermode:externalsandbox>, <urn:usf:providermode:liveexternal>;
    usf:enforcesTenantBoundary <urn:usf:tenantboundary:tenant>;
    usf:providerMaximumAttempts 3;
    usf:providerFailureBehaviour "Validate requests and responses, apply a bounded timeout, retry only classified transient failures, preserve idempotency, open a bounded circuit after repeated failure, and return a stable redacted dependency error.";
    usf:providerSecurityBoundary "Credentials are secret references scoped to this provider and environment; tenant context is explicit, outbound destinations are allow-listed, and request and response audit metadata exclude payload secrets.".
port:secretresolver usf:repositoryName "secret-resolver";
    usf:definition "Provider-independent boundary for secret resolver, owned by runtime secrets management.";
    usf:portForContract sc:runtimesecretsmanagement;
    usf:permitsProviderMode <urn:usf:providermode:deterministictestsubstitute>, <urn:usf:providermode:repositorylocalservice>;
    usf:enforcesTenantBoundary <urn:usf:tenantboundary:tenant>;
    usf:providerMaximumAttempts 3;
    usf:providerFailureBehaviour "Validate requests and responses, apply a bounded timeout, retry only classified transient failures, preserve idempotency, open a bounded circuit after repeated failure, and return a stable redacted dependency error.";
    usf:providerSecurityBoundary "Credentials are secret references scoped to this provider and environment; tenant context is explicit, outbound destinations are allow-listed, and request and response audit metadata exclude payload secrets.".
port:secretstore usf:repositoryName "secret-store";
    usf:definition "Provider-independent boundary for secret store, owned by write only secret settings.";
    usf:portForContract sc:writeonlysecretsettings;
    usf:permitsProviderMode <urn:usf:providermode:deterministictestsubstitute>, <urn:usf:providermode:repositorylocalservice>;
    usf:enforcesTenantBoundary <urn:usf:tenantboundary:tenant>;
    usf:providerMaximumAttempts 3;
    usf:providerFailureBehaviour "Validate requests and responses, apply a bounded timeout, retry only classified transient failures, preserve idempotency, open a bounded circuit after repeated failure, and return a stable redacted dependency error.";
    usf:providerSecurityBoundary "Credentials are secret references scoped to this provider and environment; tenant context is explicit, outbound destinations are allow-listed, and request and response audit metadata exclude payload secrets.".
port:sessionstore usf:repositoryName "session-store";
    usf:definition "Provider-independent boundary for session store, owned by authentication platform.";
    usf:portForContract sc:authenticationplatform;
    usf:permitsProviderMode <urn:usf:providermode:deterministictestsubstitute>, <urn:usf:providermode:repositorylocalservice>;
    usf:enforcesTenantBoundary <urn:usf:tenantboundary:tenant>;
    usf:providerMaximumAttempts 3;
    usf:providerFailureBehaviour "Validate requests and responses, apply a bounded timeout, retry only classified transient failures, preserve idempotency, open a bounded circuit after repeated failure, and return a stable redacted dependency error.";
    usf:providerSecurityBoundary "Credentials are secret references scoped to this provider and environment; tenant context is explicit, outbound destinations are allow-listed, and request and response audit metadata exclude payload secrets.".
port:siemforwarder usf:repositoryName "security-event-forwarder";
    usf:definition "Provider-independent boundary for security event forwarder, owned by audit of privileged access.";
    usf:portForContract sc:auditofprivilegedaccess;
    usf:permitsProviderMode <urn:usf:providermode:deterministictestsubstitute>, <urn:usf:providermode:repositorylocalservice>, <urn:usf:providermode:externalsandbox>, <urn:usf:providermode:liveexternal>;
    usf:enforcesTenantBoundary <urn:usf:tenantboundary:platform>;
    usf:providerMaximumAttempts 3;
    usf:providerFailureBehaviour "Validate requests and responses, apply a bounded timeout, retry only classified transient failures, preserve idempotency, open a bounded circuit after repeated failure, and return a stable redacted dependency error.";
    usf:providerSecurityBoundary "Credentials are secret references scoped to this provider and environment; tenant context is explicit, outbound destinations are allow-listed, and request and response audit metadata exclude payload secrets.".
port:signedurlissuer usf:repositoryName "signed-url-issuer";
    usf:definition "Provider-independent boundary for signed url issuer, owned by object storage tenant prefixes signed urls.";
    usf:portForContract sc:objectstorageandtenantprefixesandsignedurls;
    usf:permitsProviderMode <urn:usf:providermode:deterministictestsubstitute>, <urn:usf:providermode:repositorylocalservice>;
    usf:enforcesTenantBoundary <urn:usf:tenantboundary:tenant>;
    usf:providerMaximumAttempts 3;
    usf:providerFailureBehaviour "Validate requests and responses, apply a bounded timeout, retry only classified transient failures, preserve idempotency, open a bounded circuit after repeated failure, and return a stable redacted dependency error.";
    usf:providerSecurityBoundary "Credentials are secret references scoped to this provider and environment; tenant context is explicit, outbound destinations are allow-listed, and request and response audit metadata exclude payload secrets.".
port:telemetryport usf:repositoryName "telemetry";
    usf:definition "Provider-independent boundary for telemetry, owned by metrics traces.";
    usf:portForContract sc:metricsandtraces;
    usf:permitsProviderMode <urn:usf:providermode:deterministictestsubstitute>, <urn:usf:providermode:repositorylocalservice>;
    usf:enforcesTenantBoundary <urn:usf:tenantboundary:tenant>;
    usf:providerMaximumAttempts 3;
    usf:providerFailureBehaviour "Validate requests and responses, apply a bounded timeout, retry only classified transient failures, preserve idempotency, open a bounded circuit after repeated failure, and return a stable redacted dependency error.";
    usf:providerSecurityBoundary "Credentials are secret references scoped to this provider and environment; tenant context is explicit, outbound destinations are allow-listed, and request and response audit metadata exclude payload secrets.".
port:tenantmembershipdirectory usf:repositoryName "tenant-membership-directory";
    usf:definition "Provider-independent boundary for tenant membership directory, owned by user identity tenant membership.";
    usf:portForContract sc:useridentityandtenantmembership;
    usf:permitsProviderMode <urn:usf:providermode:deterministictestsubstitute>, <urn:usf:providermode:repositorylocalservice>;
    usf:enforcesTenantBoundary <urn:usf:tenantboundary:tenant>;
    usf:providerMaximumAttempts 3;
    usf:providerFailureBehaviour "Validate requests and responses, apply a bounded timeout, retry only classified transient failures, preserve idempotency, open a bounded circuit after repeated failure, and return a stable redacted dependency error.";
    usf:providerSecurityBoundary "Credentials are secret references scoped to this provider and environment; tenant context is explicit, outbound destinations are allow-listed, and request and response audit metadata exclude payload secrets.".
port:tenantscopedrepository usf:repositoryName "tenant-scoped-repository";
    usf:definition "Provider-independent boundary for tenant scoped repository, owned by relational storage schema changes row level security.";
    usf:portForContract sc:relationalstorageandmigrationsandrls;
    usf:permitsProviderMode <urn:usf:providermode:deterministictestsubstitute>, <urn:usf:providermode:repositorylocalservice>;
    usf:enforcesTenantBoundary <urn:usf:tenantboundary:tenant>;
    usf:providerMaximumAttempts 3;
    usf:providerFailureBehaviour "Validate requests and responses, apply a bounded timeout, retry only classified transient failures, preserve idempotency, open a bounded circuit after repeated failure, and return a stable redacted dependency error.";
    usf:providerSecurityBoundary "Credentials are secret references scoped to this provider and environment; tenant context is explicit, outbound destinations are allow-listed, and request and response audit metadata exclude payload secrets.".
port:webhookcaptureprovider usf:repositoryName "webhook-capture-provider";
    usf:definition "Provider-independent boundary for webhook capture provider, owned by developer webhooks.";
    usf:portForContract sc:webhooksdeveloperfacing;
    usf:permitsProviderMode <urn:usf:providermode:deterministictestsubstitute>, <urn:usf:providermode:repositorylocalservice>, <urn:usf:providermode:externalsandbox>, <urn:usf:providermode:liveexternal>;
    usf:enforcesTenantBoundary <urn:usf:tenantboundary:tenant>;
    usf:providerMaximumAttempts 3;
    usf:providerFailureBehaviour "Validate requests and responses, apply a bounded timeout, retry only classified transient failures, preserve idempotency, open a bounded circuit after repeated failure, and return a stable redacted dependency error.";
    usf:providerSecurityBoundary "Credentials are secret references scoped to this provider and environment; tenant context is explicit, outbound destinations are allow-listed, and request and response audit metadata exclude payload secrets.".
port:workflowengine usf:repositoryName "workflow-engine";
    usf:definition "Provider-independent boundary for workflow engine, owned by workflow engine scheduled jobs approvals.";
    usf:portForContract sc:workflowenginescheduledjobsapprovals;
    usf:permitsProviderMode <urn:usf:providermode:deterministictestsubstitute>, <urn:usf:providermode:repositorylocalservice>;
    usf:enforcesTenantBoundary <urn:usf:tenantboundary:tenant>;
    usf:providerMaximumAttempts 3;
    usf:providerFailureBehaviour "Validate requests and responses, apply a bounded timeout, retry only classified transient failures, preserve idempotency, open a bounded circuit after repeated failure, and return a stable redacted dependency error.";
    usf:providerSecurityBoundary "Credentials are secret references scoped to this provider and environment; tenant context is explicit, outbound destinations are allow-listed, and request and response audit metadata exclude payload secrets.".
op:apikeycreate usf:idempotent true;
    usf:idempotencyKeyRequired true;
    usf:timeoutMilliseconds 30000;
    usf:concurrencyBehaviour "Optimistic concurrency with explicit conflict response; no last-write-wins mutation.";
    usf:operationErrorBehaviour "Invalid input, authentication, authorisation, tenant mismatch, conflict, timeout, dependency unavailability and internal failure have distinct stable codes; failures disclose no secret or cross-tenant data and never partially mutate state.".
op:auditeventsget usf:idempotent true;
    usf:idempotencyKeyRequired false;
    usf:timeoutMilliseconds 5000;
    usf:concurrencyBehaviour "Read-only snapshot scoped to the resolved tenant and actor context.";
    usf:operationErrorBehaviour "Invalid input, authentication, authorisation, tenant mismatch, conflict, timeout, dependency unavailability and internal failure have distinct stable codes; failures disclose no secret or cross-tenant data and never partially mutate state.".
op:auditeventslist usf:idempotent true;
    usf:idempotencyKeyRequired false;
    usf:timeoutMilliseconds 5000;
    usf:concurrencyBehaviour "Read-only snapshot scoped to the resolved tenant and actor context.";
    usf:operationErrorBehaviour "Invalid input, authentication, authorisation, tenant mismatch, conflict, timeout, dependency unavailability and internal failure have distinct stable codes; failures disclose no secret or cross-tenant data and never partially mutate state.".
op:auditverifypost usf:idempotent true;
    usf:idempotencyKeyRequired false;
    usf:timeoutMilliseconds 5000;
    usf:concurrencyBehaviour "Read-only snapshot scoped to the resolved tenant and actor context.";
    usf:operationErrorBehaviour "Invalid input, authentication, authorisation, tenant mismatch, conflict, timeout, dependency unavailability and internal failure have distinct stable codes; failures disclose no secret or cross-tenant data and never partially mutate state.".
op:authloginpost usf:idempotent false;
    usf:idempotencyKeyRequired false;
    usf:timeoutMilliseconds 30000;
    usf:concurrencyBehaviour "Optimistic concurrency with explicit conflict response; no last-write-wins mutation.";
    usf:operationErrorBehaviour "Invalid input, authentication, authorisation, tenant mismatch, conflict, timeout, dependency unavailability and internal failure have distinct stable codes; failures disclose no secret or cross-tenant data and never partially mutate state.".
op:authzcheckpost usf:idempotent true;
    usf:idempotencyKeyRequired false;
    usf:timeoutMilliseconds 5000;
    usf:concurrencyBehaviour "Read-only snapshot scoped to the resolved tenant and actor context.";
    usf:operationErrorBehaviour "Invalid input, authentication, authorisation, tenant mismatch, conflict, timeout, dependency unavailability and internal failure have distinct stable codes; failures disclose no secret or cross-tenant data and never partially mutate state.".
op:authzpermissionsget usf:idempotent true;
    usf:idempotencyKeyRequired false;
    usf:timeoutMilliseconds 5000;
    usf:concurrencyBehaviour "Read-only snapshot scoped to the resolved tenant and actor context.";
    usf:operationErrorBehaviour "Invalid input, authentication, authorisation, tenant mismatch, conflict, timeout, dependency unavailability and internal failure have distinct stable codes; failures disclose no secret or cross-tenant data and never partially mutate state.".
op:configcurrentget usf:idempotent true;
    usf:idempotencyKeyRequired false;
    usf:timeoutMilliseconds 5000;
    usf:concurrencyBehaviour "Read-only snapshot scoped to the resolved tenant and actor context.";
    usf:operationErrorBehaviour "Invalid input, authentication, authorisation, tenant mismatch, conflict, timeout, dependency unavailability and internal failure have distinct stable codes; failures disclose no secret or cross-tenant data and never partially mutate state.".
op:configflagsget usf:idempotent true;
    usf:idempotencyKeyRequired false;
    usf:timeoutMilliseconds 5000;
    usf:concurrencyBehaviour "Read-only snapshot scoped to the resolved tenant and actor context.";
    usf:operationErrorBehaviour "Invalid input, authentication, authorisation, tenant mismatch, conflict, timeout, dependency unavailability and internal failure have distinct stable codes; failures disclose no secret or cross-tenant data and never partially mutate state.".
op:configproviderstatusget usf:idempotent true;
    usf:idempotencyKeyRequired false;
    usf:timeoutMilliseconds 5000;
    usf:concurrencyBehaviour "Read-only snapshot scoped to the resolved tenant and actor context.";
    usf:operationErrorBehaviour "Invalid input, authentication, authorisation, tenant mismatch, conflict, timeout, dependency unavailability and internal failure have distinct stable codes; failures disclose no secret or cross-tenant data and never partially mutate state.".
op:filesdownload usf:idempotent true;
    usf:idempotencyKeyRequired false;
    usf:timeoutMilliseconds 5000;
    usf:concurrencyBehaviour "Read-only snapshot scoped to the resolved tenant and actor context.";
    usf:operationErrorBehaviour "Invalid input, authentication, authorisation, tenant mismatch, conflict, timeout, dependency unavailability and internal failure have distinct stable codes; failures disclose no secret or cross-tenant data and never partially mutate state.".
op:filesget usf:idempotent true;
    usf:idempotencyKeyRequired false;
    usf:timeoutMilliseconds 5000;
    usf:concurrencyBehaviour "Read-only snapshot scoped to the resolved tenant and actor context.";
    usf:operationErrorBehaviour "Invalid input, authentication, authorisation, tenant mismatch, conflict, timeout, dependency unavailability and internal failure have distinct stable codes; failures disclose no secret or cross-tenant data and never partially mutate state.".
op:fileslist usf:idempotent true;
    usf:idempotencyKeyRequired false;
    usf:timeoutMilliseconds 5000;
    usf:concurrencyBehaviour "Read-only snapshot scoped to the resolved tenant and actor context.";
    usf:operationErrorBehaviour "Invalid input, authentication, authorisation, tenant mismatch, conflict, timeout, dependency unavailability and internal failure have distinct stable codes; failures disclose no secret or cross-tenant data and never partially mutate state.".
op:filesupload usf:idempotent true;
    usf:idempotencyKeyRequired true;
    usf:timeoutMilliseconds 30000;
    usf:concurrencyBehaviour "Optimistic concurrency with explicit conflict response; no last-write-wins mutation.";
    usf:operationErrorBehaviour "Invalid input, authentication, authorisation, tenant mismatch, conflict, timeout, dependency unavailability and internal failure have distinct stable codes; failures disclose no secret or cross-tenant data and never partially mutate state.".
op:filesverify usf:idempotent true;
    usf:idempotencyKeyRequired false;
    usf:timeoutMilliseconds 5000;
    usf:concurrencyBehaviour "Read-only snapshot scoped to the resolved tenant and actor context.";
    usf:operationErrorBehaviour "Invalid input, authentication, authorisation, tenant mismatch, conflict, timeout, dependency unavailability and internal failure have distinct stable codes; failures disclose no secret or cross-tenant data and never partially mutate state.".
op:healthzget usf:idempotent true;
    usf:idempotencyKeyRequired false;
    usf:timeoutMilliseconds 5000;
    usf:concurrencyBehaviour "Read-only snapshot scoped to the resolved tenant and actor context.";
    usf:operationErrorBehaviour "Invalid input, authentication, authorisation, tenant mismatch, conflict, timeout, dependency unavailability and internal failure have distinct stable codes; failures disclose no secret or cross-tenant data and never partially mutate state.".
op:jobscancel usf:idempotent true;
    usf:idempotencyKeyRequired true;
    usf:timeoutMilliseconds 30000;
    usf:concurrencyBehaviour "Optimistic concurrency with explicit conflict response; no last-write-wins mutation.";
    usf:operationErrorBehaviour "Invalid input, authentication, authorisation, tenant mismatch, conflict, timeout, dependency unavailability and internal failure have distinct stable codes; failures disclose no secret or cross-tenant data and never partially mutate state.".
op:jobscreate usf:idempotent true;
    usf:idempotencyKeyRequired true;
    usf:timeoutMilliseconds 30000;
    usf:concurrencyBehaviour "Optimistic concurrency with explicit conflict response; no last-write-wins mutation.";
    usf:operationErrorBehaviour "Invalid input, authentication, authorisation, tenant mismatch, conflict, timeout, dependency unavailability and internal failure have distinct stable codes; failures disclose no secret or cross-tenant data and never partially mutate state.".
op:jobsget usf:idempotent true;
    usf:idempotencyKeyRequired false;
    usf:timeoutMilliseconds 5000;
    usf:concurrencyBehaviour "Read-only snapshot scoped to the resolved tenant and actor context.";
    usf:operationErrorBehaviour "Invalid input, authentication, authorisation, tenant mismatch, conflict, timeout, dependency unavailability and internal failure have distinct stable codes; failures disclose no secret or cross-tenant data and never partially mutate state.".
op:jobslist usf:idempotent true;
    usf:idempotencyKeyRequired false;
    usf:timeoutMilliseconds 5000;
    usf:concurrencyBehaviour "Read-only snapshot scoped to the resolved tenant and actor context.";
    usf:operationErrorBehaviour "Invalid input, authentication, authorisation, tenant mismatch, conflict, timeout, dependency unavailability and internal failure have distinct stable codes; failures disclose no secret or cross-tenant data and never partially mutate state.".
op:notificationscancel usf:idempotent true;
    usf:idempotencyKeyRequired true;
    usf:timeoutMilliseconds 30000;
    usf:concurrencyBehaviour "Optimistic concurrency with explicit conflict response; no last-write-wins mutation.";
    usf:operationErrorBehaviour "Invalid input, authentication, authorisation, tenant mismatch, conflict, timeout, dependency unavailability and internal failure have distinct stable codes; failures disclose no secret or cross-tenant data and never partially mutate state.".
op:notificationscreate usf:idempotent true;
    usf:idempotencyKeyRequired true;
    usf:timeoutMilliseconds 30000;
    usf:concurrencyBehaviour "Optimistic concurrency with explicit conflict response; no last-write-wins mutation.";
    usf:operationErrorBehaviour "Invalid input, authentication, authorisation, tenant mismatch, conflict, timeout, dependency unavailability and internal failure have distinct stable codes; failures disclose no secret or cross-tenant data and never partially mutate state.".
op:notificationsget usf:idempotent true;
    usf:idempotencyKeyRequired false;
    usf:timeoutMilliseconds 5000;
    usf:concurrencyBehaviour "Read-only snapshot scoped to the resolved tenant and actor context.";
    usf:operationErrorBehaviour "Invalid input, authentication, authorisation, tenant mismatch, conflict, timeout, dependency unavailability and internal failure have distinct stable codes; failures disclose no secret or cross-tenant data and never partially mutate state.".
op:notificationslist usf:idempotent true;
    usf:idempotencyKeyRequired false;
    usf:timeoutMilliseconds 5000;
    usf:concurrencyBehaviour "Read-only snapshot scoped to the resolved tenant and actor context.";
    usf:operationErrorBehaviour "Invalid input, authentication, authorisation, tenant mismatch, conflict, timeout, dependency unavailability and internal failure have distinct stable codes; failures disclose no secret or cross-tenant data and never partially mutate state.".
op:notificationssend usf:idempotent true;
    usf:idempotencyKeyRequired true;
    usf:timeoutMilliseconds 30000;
    usf:concurrencyBehaviour "Optimistic concurrency with explicit conflict response; no last-write-wins mutation.";
    usf:operationErrorBehaviour "Invalid input, authentication, authorisation, tenant mismatch, conflict, timeout, dependency unavailability and internal failure have distinct stable codes; failures disclose no secret or cross-tenant data and never partially mutate state.".
op:notificationtemplatescreate usf:idempotent true;
    usf:idempotencyKeyRequired true;
    usf:timeoutMilliseconds 30000;
    usf:concurrencyBehaviour "Optimistic concurrency with explicit conflict response; no last-write-wins mutation.";
    usf:operationErrorBehaviour "Invalid input, authentication, authorisation, tenant mismatch, conflict, timeout, dependency unavailability and internal failure have distinct stable codes; failures disclose no secret or cross-tenant data and never partially mutate state.".
op:observabilityreadinessget usf:idempotent true;
    usf:idempotencyKeyRequired false;
    usf:timeoutMilliseconds 5000;
    usf:concurrencyBehaviour "Read-only snapshot scoped to the resolved tenant and actor context.";
    usf:operationErrorBehaviour "Invalid input, authentication, authorisation, tenant mismatch, conflict, timeout, dependency unavailability and internal failure have distinct stable codes; failures disclose no secret or cross-tenant data and never partially mutate state.".
op:observabilitysignalslist usf:idempotent true;
    usf:idempotencyKeyRequired false;
    usf:timeoutMilliseconds 5000;
    usf:concurrencyBehaviour "Read-only snapshot scoped to the resolved tenant and actor context.";
    usf:operationErrorBehaviour "Invalid input, authentication, authorisation, tenant mismatch, conflict, timeout, dependency unavailability and internal failure have distinct stable codes; failures disclose no secret or cross-tenant data and never partially mutate state.".
op:openapiget usf:idempotent true;
    usf:idempotencyKeyRequired false;
    usf:timeoutMilliseconds 5000;
    usf:concurrencyBehaviour "Read-only snapshot scoped to the resolved tenant and actor context.";
    usf:operationErrorBehaviour "Invalid input, authentication, authorisation, tenant mismatch, conflict, timeout, dependency unavailability and internal failure have distinct stable codes; failures disclose no secret or cross-tenant data and never partially mutate state.".
op:profiledetailget usf:idempotent true;
    usf:idempotencyKeyRequired false;
    usf:timeoutMilliseconds 5000;
    usf:concurrencyBehaviour "Read-only snapshot scoped to the resolved tenant and actor context.";
    usf:operationErrorBehaviour "Invalid input, authentication, authorisation, tenant mismatch, conflict, timeout, dependency unavailability and internal failure have distinct stable codes; failures disclose no secret or cross-tenant data and never partially mutate state.".
op:profilelistget usf:idempotent true;
    usf:idempotencyKeyRequired false;
    usf:timeoutMilliseconds 5000;
    usf:concurrencyBehaviour "Read-only snapshot scoped to the resolved tenant and actor context.";
    usf:operationErrorBehaviour "Invalid input, authentication, authorisation, tenant mismatch, conflict, timeout, dependency unavailability and internal failure have distinct stable codes; failures disclose no secret or cross-tenant data and never partially mutate state.".
op:providersget usf:idempotent true;
    usf:idempotencyKeyRequired false;
    usf:timeoutMilliseconds 5000;
    usf:concurrencyBehaviour "Read-only snapshot scoped to the resolved tenant and actor context.";
    usf:operationErrorBehaviour "Invalid input, authentication, authorisation, tenant mismatch, conflict, timeout, dependency unavailability and internal failure have distinct stable codes; failures disclose no secret or cross-tenant data and never partially mutate state.".
op:providerslist usf:idempotent true;
    usf:idempotencyKeyRequired false;
    usf:timeoutMilliseconds 5000;
    usf:concurrencyBehaviour "Read-only snapshot scoped to the resolved tenant and actor context.";
    usf:operationErrorBehaviour "Invalid input, authentication, authorisation, tenant mismatch, conflict, timeout, dependency unavailability and internal failure have distinct stable codes; failures disclose no secret or cross-tenant data and never partially mutate state.".
op:readyzget usf:idempotent true;
    usf:idempotencyKeyRequired false;
    usf:timeoutMilliseconds 5000;
    usf:concurrencyBehaviour "Read-only snapshot scoped to the resolved tenant and actor context.";
    usf:operationErrorBehaviour "Invalid input, authentication, authorisation, tenant mismatch, conflict, timeout, dependency unavailability and internal failure have distinct stable codes; failures disclose no secret or cross-tenant data and never partially mutate state.".
op:tenantcontextget usf:idempotent true;
    usf:idempotencyKeyRequired false;
    usf:timeoutMilliseconds 5000;
    usf:concurrencyBehaviour "Read-only snapshot scoped to the resolved tenant and actor context.";
    usf:operationErrorBehaviour "Invalid input, authentication, authorisation, tenant mismatch, conflict, timeout, dependency unavailability and internal failure have distinct stable codes; failures disclose no secret or cross-tenant data and never partially mutate state.".
iface:abacpolicydecisionpointoperations a usf:Interface;
    usf:canonicalName "abacpolicydecisionpointoperations";
    usf:repositoryName "attribute-based-access-control-policy-decision-point-operations";
    usf:definition "Callable command and query surface owned by attribute based access control policy decision point.";
    usf:interfaceForContract sc:abacpolicydecisionpoint;
    usf:hasOperation op:authzcheckpost.
iface:apikeyspersonalaccesstokensoperations a usf:Interface;
    usf:canonicalName "apikeyspersonalaccesstokensoperations";
    usf:repositoryName "api-keys-personal-access-tokens-operations";
    usf:definition "Callable command and query surface owned by api keys personal access tokens.";
    usf:interfaceForContract sc:apikeyspersonalaccesstokens;
    usf:hasOperation op:apikeycreate.
iface:auditofprivilegedaccessoperations a usf:Interface;
    usf:canonicalName "auditofprivilegedaccessoperations";
    usf:repositoryName "audit-of-privileged-access-operations";
    usf:definition "Callable command and query surface owned by audit of privileged access.";
    usf:interfaceForContract sc:auditofprivilegedaccess;
    usf:hasOperation op:auditeventsget, op:auditeventslist, op:auditverifypost.
iface:authenticationplatformoperations a usf:Interface;
    usf:canonicalName "authenticationplatformoperations";
    usf:repositoryName "authentication-platform-operations";
    usf:definition "Callable command and query surface owned by authentication platform.";
    usf:interfaceForContract sc:authenticationplatform;
    usf:hasOperation op:authloginpost.
iface:configurationregistryandhistoryoperations a usf:Interface;
    usf:canonicalName "configurationregistryandhistoryoperations";
    usf:repositoryName "configuration-registry-history-operations";
    usf:definition "Callable command and query surface owned by configuration registry history.";
    usf:interfaceForContract sc:configurationregistryandhistory;
    usf:hasOperation op:configcurrentget, op:configflagsget.
iface:enduserprofileandpreferencesselfserviceoperations a usf:Interface;
    usf:canonicalName "enduserprofileandpreferencesselfserviceoperations";
    usf:repositoryName "end-user-profile-preferences-self-service-operations";
    usf:definition "Callable command and query surface owned by end user profile preferences self service.";
    usf:interfaceForContract sc:enduserprofileandpreferencesselfservice;
    usf:hasOperation op:profiledetailget, op:profilelistget.
iface:internalservicecatalogandreadinessoperations a usf:Interface;
    usf:canonicalName "internalservicecatalogandreadinessoperations";
    usf:repositoryName "internal-service-catalog-readiness-operations";
    usf:definition "Callable command and query surface owned by internal service catalog readiness.";
    usf:interfaceForContract sc:internalservicecatalogandreadiness;
    usf:hasOperation op:healthzget, op:readyzget.
iface:metricsandtracesoperations a usf:Interface;
    usf:canonicalName "metricsandtracesoperations";
    usf:repositoryName "metrics-traces-operations";
    usf:definition "Callable command and query surface owned by metrics traces.";
    usf:interfaceForContract sc:metricsandtraces;
    usf:hasOperation op:observabilityreadinessget, op:observabilitysignalslist.
iface:notificationdeliveryandpreferencesandchannelsoperations a usf:Interface;
    usf:canonicalName "notificationdeliveryandpreferencesandchannelsoperations";
    usf:repositoryName "notification-delivery-preferences-channels-operations";
    usf:definition "Callable command and query surface owned by notification delivery preferences channels.";
    usf:interfaceForContract sc:notificationdeliveryandpreferencesandchannels;
    usf:hasOperation op:notificationscancel, op:notificationscreate, op:notificationsget, op:notificationslist, op:notificationssend, op:notificationtemplatescreate.
iface:objectstorageandtenantprefixesandsignedurlsoperations a usf:Interface;
    usf:canonicalName "objectstorageandtenantprefixesandsignedurlsoperations";
    usf:repositoryName "object-storage-tenant-prefixes-signed-urls-operations";
    usf:definition "Callable command and query surface owned by object storage tenant prefixes signed urls.";
    usf:interfaceForContract sc:objectstorageandtenantprefixesandsignedurls;
    usf:hasOperation op:filesdownload, op:filesget, op:fileslist, op:filesupload, op:filesverify.
iface:openapidrifthardgateoperations a usf:Interface;
    usf:canonicalName "openapidrifthardgateoperations";
    usf:repositoryName "open-api-drift-gate-operations";
    usf:definition "Callable command and query surface owned by open api drift gate.";
    usf:interfaceForContract sc:openapidrifthardgate;
    usf:hasOperation op:openapiget.
iface:providerconfigurationplaneoperations a usf:Interface;
    usf:canonicalName "providerconfigurationplaneoperations";
    usf:repositoryName "provider-configuration-plane-operations";
    usf:definition "Callable command and query surface owned by provider configuration plane.";
    usf:interfaceForContract sc:providerconfigurationplane;
    usf:hasOperation op:providersget, op:providerslist.
iface:providerenvironmentclassificationoperations a usf:Interface;
    usf:canonicalName "providerenvironmentclassificationoperations";
    usf:repositoryName "provider-environment-classification-operations";
    usf:definition "Callable command and query surface owned by provider environment classification.";
    usf:interfaceForContract sc:providerenvironmentclassification;
    usf:hasOperation op:configproviderstatusget.
iface:rbacrolesandpermissionsoperations a usf:Interface;
    usf:canonicalName "rbacrolesandpermissionsoperations";
    usf:repositoryName "role-based-access-control-roles-permissions-operations";
    usf:definition "Callable command and query surface owned by role based access control roles permissions.";
    usf:interfaceForContract sc:rbacrolesandpermissions;
    usf:hasOperation op:authzpermissionsget.
iface:scheduledjobsbuiltinontheeventsubstrateoperations a usf:Interface;
    usf:canonicalName "scheduledjobsbuiltinontheeventsubstrateoperations";
    usf:repositoryName "scheduled-jobs-event-substrate-operations";
    usf:definition "Callable command and query surface owned by scheduled jobs event substrate.";
    usf:interfaceForContract sc:scheduledjobsbuiltinontheeventsubstrate;
    usf:hasOperation op:jobscancel, op:jobscreate, op:jobsget, op:jobslist.
iface:tenanthostidentityresolutionoperations a usf:Interface;
    usf:canonicalName "tenanthostidentityresolutionoperations";
    usf:repositoryName "tenant-host-identity-resolution-operations";
    usf:definition "Callable command and query surface owned by tenant host identity resolution.";
    usf:interfaceForContract sc:tenanthostidentityresolution;
    usf:hasOperation op:tenantcontextget.
port:analyticseventstore usf:repositoryName "analytics-event-store";
    usf:definition "Provider-independent boundary for analytics event store, owned by usage metering meter event ingestion.";
    usf:portForContract sc:usagemeteringandmetereventingestion;
    usf:permitsProviderMode <urn:usf:providermode:deterministictestsubstitute>, <urn:usf:providermode:repositorylocalservice>, <urn:usf:providermode:externalsandbox>, <urn:usf:providermode:liveexternal>;
    usf:enforcesTenantBoundary <urn:usf:tenantboundary:tenant>;
    usf:providerMaximumAttempts 3;
    usf:providerFailureBehaviour "Validate requests and responses, apply a bounded timeout, retry only classified transient failures, preserve idempotency, open a bounded circuit after repeated failure, and return a stable redacted dependency error.";
    usf:providerSecurityBoundary "Credentials are secret references scoped to this provider and environment; tenant context is explicit, outbound destinations are allow-listed, and request and response audit metadata exclude payload secrets.".
port:apiruntime usf:repositoryName "api-runtime";
    usf:definition "Provider-independent boundary for api runtime, owned by universal service foundation scope principles.";
    usf:portForContract sc:universalservicefoundationscopeandprinciples;
    usf:permitsProviderMode <urn:usf:providermode:deterministictestsubstitute>, <urn:usf:providermode:repositorylocalservice>;
    usf:enforcesTenantBoundary <urn:usf:tenantboundary:tenant>;
    usf:providerMaximumAttempts 3;
    usf:providerFailureBehaviour "Validate requests and responses, apply a bounded timeout, retry only classified transient failures, preserve idempotency, open a bounded circuit after repeated failure, and return a stable redacted dependency error.";
    usf:providerSecurityBoundary "Credentials are secret references scoped to this provider and environment; tenant context is explicit, outbound destinations are allow-listed, and request and response audit metadata exclude payload secrets.".
port:auditeventledger usf:repositoryName "audit-event-ledger";
    usf:definition "Provider-independent boundary for audit event ledger, owned by audit of privileged access.";
    usf:portForContract sc:auditofprivilegedaccess;
    usf:permitsProviderMode <urn:usf:providermode:deterministictestsubstitute>, <urn:usf:providermode:repositorylocalservice>;
    usf:enforcesTenantBoundary <urn:usf:tenantboundary:tenant>;
    usf:providerMaximumAttempts 3;
    usf:providerFailureBehaviour "Validate requests and responses, apply a bounded timeout, retry only classified transient failures, preserve idempotency, open a bounded circuit after repeated failure, and return a stable redacted dependency error.";
    usf:providerSecurityBoundary "Credentials are secret references scoped to this provider and environment; tenant context is explicit, outbound destinations are allow-listed, and request and response audit metadata exclude payload secrets.".
port:auditexporter usf:repositoryName "audit-exporter";
    usf:definition "Provider-independent boundary for audit exporter, owned by audit of privileged access.";
    usf:portForContract sc:auditofprivilegedaccess;
    usf:permitsProviderMode <urn:usf:providermode:deterministictestsubstitute>, <urn:usf:providermode:repositorylocalservice>, <urn:usf:providermode:externalsandbox>, <urn:usf:providermode:liveexternal>;
    usf:enforcesTenantBoundary <urn:usf:tenantboundary:platform>;
    usf:providerMaximumAttempts 3;
    usf:providerFailureBehaviour "Validate requests and responses, apply a bounded timeout, retry only classified transient failures, preserve idempotency, open a bounded circuit after repeated failure, and return a stable redacted dependency error.";
    usf:providerSecurityBoundary "Credentials are secret references scoped to this provider and environment; tenant context is explicit, outbound destinations are allow-listed, and request and response audit metadata exclude payload secrets.".
port:auditledger usf:repositoryName "audit-ledger";
    usf:definition "Provider-independent boundary for audit ledger, owned by audit of privileged access.";
    usf:portForContract sc:auditofprivilegedaccess;
    usf:permitsProviderMode <urn:usf:providermode:deterministictestsubstitute>, <urn:usf:providermode:repositorylocalservice>;
    usf:enforcesTenantBoundary <urn:usf:tenantboundary:tenant>;
    usf:providerMaximumAttempts 3;
    usf:providerFailureBehaviour "Validate requests and responses, apply a bounded timeout, retry only classified transient failures, preserve idempotency, open a bounded circuit after repeated failure, and return a stable redacted dependency error.";
    usf:providerSecurityBoundary "Credentials are secret references scoped to this provider and environment; tenant context is explicit, outbound destinations are allow-listed, and request and response audit metadata exclude payload secrets.".
port:auditrecorder usf:repositoryName "audit-recorder";
    usf:definition "Provider-independent boundary for audit recorder, owned by audit of privileged access.";
    usf:portForContract sc:auditofprivilegedaccess;
    usf:permitsProviderMode <urn:usf:providermode:deterministictestsubstitute>, <urn:usf:providermode:repositorylocalservice>;
    usf:enforcesTenantBoundary <urn:usf:tenantboundary:tenant>;
    usf:providerMaximumAttempts 3;
    usf:providerFailureBehaviour "Validate requests and responses, apply a bounded timeout, retry only classified transient failures, preserve idempotency, open a bounded circuit after repeated failure, and return a stable redacted dependency error.";
    usf:providerSecurityBoundary "Credentials are secret references scoped to this provider and environment; tenant context is explicit, outbound destinations are allow-listed, and request and response audit metadata exclude payload secrets.".
port:auditsigner usf:repositoryName "audit-signer";
    usf:definition "Provider-independent boundary for audit signer, owned by audit of privileged access.";
    usf:portForContract sc:auditofprivilegedaccess;
    usf:permitsProviderMode <urn:usf:providermode:deterministictestsubstitute>, <urn:usf:providermode:repositorylocalservice>;
    usf:enforcesTenantBoundary <urn:usf:tenantboundary:platform>;
    usf:providerMaximumAttempts 3;
    usf:providerFailureBehaviour "Validate requests and responses, apply a bounded timeout, retry only classified transient failures, preserve idempotency, open a bounded circuit after repeated failure, and return a stable redacted dependency error.";
    usf:providerSecurityBoundary "Credentials are secret references scoped to this provider and environment; tenant context is explicit, outbound destinations are allow-listed, and request and response audit metadata exclude payload secrets.".
port:backuprestoreprovider usf:repositoryName "backup-restore-provider";
    usf:definition "Provider-independent boundary for backup restore provider, owned by backup and restore.";
    usf:portForContract sc:backupandrestore;
    usf:permitsProviderMode <urn:usf:providermode:deterministictestsubstitute>, <urn:usf:providermode:repositorylocalservice>, <urn:usf:providermode:externalsandbox>, <urn:usf:providermode:liveexternal>;
    usf:enforcesTenantBoundary <urn:usf:tenantboundary:platform>;
    usf:providerMaximumAttempts 3;
    usf:providerFailureBehaviour "Validate requests and responses, apply a bounded timeout, retry only classified transient failures, preserve idempotency, open a bounded circuit after repeated failure, and return a stable redacted dependency error.";
    usf:providerSecurityBoundary "Credentials are secret references scoped to this provider and environment; tenant context is explicit, outbound destinations are allow-listed, and request and response audit metadata exclude payload secrets.".
port:cacheprovider usf:repositoryName "cache-provider";
    usf:definition "Provider-independent boundary for cache provider, owned by service catalog provider integration model.";
    usf:portForContract sc:servicecatalogandproviderintegrationmodel;
    usf:permitsProviderMode <urn:usf:providermode:deterministictestsubstitute>, <urn:usf:providermode:repositorylocalservice>;
    usf:enforcesTenantBoundary <urn:usf:tenantboundary:tenant>;
    usf:providerMaximumAttempts 3;
    usf:providerFailureBehaviour "Validate requests and responses, apply a bounded timeout, retry only classified transient failures, preserve idempotency, open a bounded circuit after repeated failure, and return a stable redacted dependency error.";
    usf:providerSecurityBoundary "Credentials are secret references scoped to this provider and environment; tenant context is explicit, outbound destinations are allow-listed, and request and response audit metadata exclude payload secrets.".
port:cloudprovideremulator usf:repositoryName "cloud-provider-emulator";
    usf:definition "Provider-independent boundary for cloud provider emulator, owned by deterministic development test substitutes.";
    usf:portForContract sc:deterministicdevelopmentandtestsubstitutes;
    usf:permitsProviderMode <urn:usf:providermode:deterministictestsubstitute>, <urn:usf:providermode:repositorylocalservice>;
    usf:enforcesTenantBoundary <urn:usf:tenantboundary:platform>;
    usf:providerMaximumAttempts 3;
    usf:providerFailureBehaviour "Validate requests and responses, apply a bounded timeout, retry only classified transient failures, preserve idempotency, open a bounded circuit after repeated failure, and return a stable redacted dependency error.";
    usf:providerSecurityBoundary "Credentials are secret references scoped to this provider and environment; tenant context is explicit, outbound destinations are allow-listed, and request and response audit metadata exclude payload secrets.".
port:configlayerprovider usf:repositoryName "configuration-layer-provider";
    usf:definition "Provider-independent boundary for configuration layer provider, owned by configuration registry history.";
    usf:portForContract sc:configurationregistryandhistory;
    usf:permitsProviderMode <urn:usf:providermode:deterministictestsubstitute>, <urn:usf:providermode:repositorylocalservice>;
    usf:enforcesTenantBoundary <urn:usf:tenantboundary:tenant>;
    usf:providerMaximumAttempts 3;
    usf:providerFailureBehaviour "Validate requests and responses, apply a bounded timeout, retry only classified transient failures, preserve idempotency, open a bounded circuit after repeated failure, and return a stable redacted dependency error.";
    usf:providerSecurityBoundary "Credentials are secret references scoped to this provider and environment; tenant context is explicit, outbound destinations are allow-listed, and request and response audit metadata exclude payload secrets.".
port:durableworkflowport usf:repositoryName "durable-workflow";
    usf:definition "Provider-independent boundary for durable workflow, owned by workflow engine scheduled jobs approvals.";
    usf:portForContract sc:workflowenginescheduledjobsapprovals;
    usf:permitsProviderMode <urn:usf:providermode:deterministictestsubstitute>, <urn:usf:providermode:repositorylocalservice>;
    usf:enforcesTenantBoundary <urn:usf:tenantboundary:tenant>;
    usf:providerMaximumAttempts 3;
    usf:providerFailureBehaviour "Validate requests and responses, apply a bounded timeout, retry only classified transient failures, preserve idempotency, open a bounded circuit after repeated failure, and return a stable redacted dependency error.";
    usf:providerSecurityBoundary "Credentials are secret references scoped to this provider and environment; tenant context is explicit, outbound destinations are allow-listed, and request and response audit metadata exclude payload secrets.".
port:eventbus usf:repositoryName "event-bus";
    usf:definition "Provider-independent boundary for event bus, owned by event bus durable queues dead letter redrive.";
    usf:portForContract sc:eventbusdurablequeuesdlqredrive;
    usf:permitsProviderMode <urn:usf:providermode:deterministictestsubstitute>, <urn:usf:providermode:repositorylocalservice>;
    usf:enforcesTenantBoundary <urn:usf:tenantboundary:tenant>;
    usf:providerMaximumAttempts 3;
    usf:providerFailureBehaviour "Validate requests and responses, apply a bounded timeout, retry only classified transient failures, preserve idempotency, open a bounded circuit after repeated failure, and return a stable redacted dependency error.";
    usf:providerSecurityBoundary "Credentials are secret references scoped to this provider and environment; tenant context is explicit, outbound destinations are allow-listed, and request and response audit metadata exclude payload secrets.".
port:externalsecretmanager usf:repositoryName "external-secret-manager";
    usf:definition "Provider-independent boundary for external secret manager, owned by runtime secrets management.";
    usf:portForContract sc:runtimesecretsmanagement;
    usf:permitsProviderMode <urn:usf:providermode:deterministictestsubstitute>, <urn:usf:providermode:repositorylocalservice>, <urn:usf:providermode:externalsandbox>, <urn:usf:providermode:liveexternal>;
    usf:enforcesTenantBoundary <urn:usf:tenantboundary:platform>;
    usf:providerMaximumAttempts 3;
    usf:providerFailureBehaviour "Validate requests and responses, apply a bounded timeout, retry only classified transient failures, preserve idempotency, open a bounded circuit after repeated failure, and return a stable redacted dependency error.";
    usf:providerSecurityBoundary "Credentials are secret references scoped to this provider and environment; tenant context is explicit, outbound destinations are allow-listed, and request and response audit metadata exclude payload secrets.".
port:featureflagsource usf:repositoryName "feature-flag-source";
    usf:definition "Provider-independent boundary for feature flag source, owned by configuration registry history.";
    usf:portForContract sc:configurationregistryandhistory;
    usf:permitsProviderMode <urn:usf:providermode:deterministictestsubstitute>, <urn:usf:providermode:repositorylocalservice>;
    usf:enforcesTenantBoundary <urn:usf:tenantboundary:tenant>;
    usf:providerMaximumAttempts 3;
    usf:providerFailureBehaviour "Validate requests and responses, apply a bounded timeout, retry only classified transient failures, preserve idempotency, open a bounded circuit after repeated failure, and return a stable redacted dependency error.";
    usf:providerSecurityBoundary "Credentials are secret references scoped to this provider and environment; tenant context is explicit, outbound destinations are allow-listed, and request and response audit metadata exclude payload secrets.".
port:filemetadatastore usf:repositoryName "file-metadata-store";
    usf:definition "Provider-independent boundary for file metadata store, owned by object storage tenant prefixes signed urls.";
    usf:portForContract sc:objectstorageandtenantprefixesandsignedurls;
    usf:permitsProviderMode <urn:usf:providermode:deterministictestsubstitute>, <urn:usf:providermode:repositorylocalservice>;
    usf:enforcesTenantBoundary <urn:usf:tenantboundary:tenant>;
    usf:providerMaximumAttempts 3;
    usf:providerFailureBehaviour "Validate requests and responses, apply a bounded timeout, retry only classified transient failures, preserve idempotency, open a bounded circuit after repeated failure, and return a stable redacted dependency error.";
    usf:providerSecurityBoundary "Credentials are secret references scoped to this provider and environment; tenant context is explicit, outbound destinations are allow-listed, and request and response audit metadata exclude payload secrets.".
port:guardrailport usf:repositoryName "guardrail";
    usf:definition "Provider-independent boundary for guardrail, owned by code quality secret dependency scanning.";
    usf:portForContract sc:codequalityandsecretanddependencyscanning;
    usf:permitsProviderMode <urn:usf:providermode:deterministictestsubstitute>, <urn:usf:providermode:repositorylocalservice>;
    usf:enforcesTenantBoundary <urn:usf:tenantboundary:platform>;
    usf:providerMaximumAttempts 3;
    usf:providerFailureBehaviour "Validate requests and responses, apply a bounded timeout, retry only classified transient failures, preserve idempotency, open a bounded circuit after repeated failure, and return a stable redacted dependency error.";
    usf:providerSecurityBoundary "Credentials are secret references scoped to this provider and environment; tenant context is explicit, outbound destinations are allow-listed, and request and response audit metadata exclude payload secrets.".
port:identitydirectory usf:repositoryName "identity-directory";
    usf:definition "Provider-independent boundary for identity directory, owned by user identity tenant membership.";
    usf:portForContract sc:useridentityandtenantmembership;
    usf:permitsProviderMode <urn:usf:providermode:deterministictestsubstitute>, <urn:usf:providermode:repositorylocalservice>;
    usf:enforcesTenantBoundary <urn:usf:tenantboundary:tenant>;
    usf:providerMaximumAttempts 3;
    usf:providerFailureBehaviour "Validate requests and responses, apply a bounded timeout, retry only classified transient failures, preserve idempotency, open a bounded circuit after repeated failure, and return a stable redacted dependency error.";
    usf:providerSecurityBoundary "Credentials are secret references scoped to this provider and environment; tenant context is explicit, outbound destinations are allow-listed, and request and response audit metadata exclude payload secrets.".
port:identityprovider usf:repositoryName "identity-provider";
    usf:definition "Provider-independent boundary for identity provider, owned by authentication platform.";
    usf:portForContract sc:authenticationplatform;
    usf:permitsProviderMode <urn:usf:providermode:deterministictestsubstitute>, <urn:usf:providermode:repositorylocalservice>, <urn:usf:providermode:externalsandbox>, <urn:usf:providermode:liveexternal>;
    usf:enforcesTenantBoundary <urn:usf:tenantboundary:tenant>;
    usf:providerMaximumAttempts 3;
    usf:providerFailureBehaviour "Retry only transient discovery, signing-key retrieval or provider-network failures with bounded backoff. Invalid signature, issuer, audience, nonce, state, expiry or other credential failures are non-retryable and fail closed. No failed verification creates a session; errors are stable and redact tokens and provider detail.";
    usf:providerSecurityBoundary "Provider configuration and credentials are tenant- and environment-scoped; issuer, audience, signature, expiry, not-before, nonce, state and proof-key exchange are validated; endpoints are allow-listed and transport encryption is mandatory except for explicitly authorised local substitutes; raw tokens, secrets and cross-tenant material never enter logs or responses.".
port:importexportport usf:repositoryName "import-export";
    usf:definition "Provider-independent boundary for import export, owned by tenant data import export.";
    usf:portForContract sc:tenantdataimportexport;
    usf:permitsProviderMode <urn:usf:providermode:deterministictestsubstitute>, <urn:usf:providermode:repositorylocalservice>;
    usf:enforcesTenantBoundary <urn:usf:tenantboundary:tenant>;
    usf:providerMaximumAttempts 3;
    usf:providerFailureBehaviour "Validate requests and responses, apply a bounded timeout, retry only classified transient failures, preserve idempotency, open a bounded circuit after repeated failure, and return a stable redacted dependency error.";
    usf:providerSecurityBoundary "Credentials are secret references scoped to this provider and environment; tenant context is explicit, outbound destinations are allow-listed, and request and response audit metadata exclude payload secrets.".
port:mailprovider usf:repositoryName "mail-provider";
    usf:definition "Provider-independent boundary for mail provider, owned by notification delivery preferences channels.";
    usf:portForContract sc:notificationdeliveryandpreferencesandchannels;
    usf:permitsProviderMode <urn:usf:providermode:deterministictestsubstitute>, <urn:usf:providermode:repositorylocalservice>, <urn:usf:providermode:externalsandbox>, <urn:usf:providermode:liveexternal>;
    usf:enforcesTenantBoundary <urn:usf:tenantboundary:tenant>;
    usf:providerMaximumAttempts 3;
    usf:providerFailureBehaviour "Validate requests and responses, apply a bounded timeout, retry only classified transient failures, preserve idempotency, open a bounded circuit after repeated failure, and return a stable redacted dependency error.";
    usf:providerSecurityBoundary "Credentials are secret references scoped to this provider and environment; tenant context is explicit, outbound destinations are allow-listed, and request and response audit metadata exclude payload secrets.".
port:notificationprovider usf:repositoryName "notification-provider";
    usf:definition "Provider-independent boundary for notification provider, owned by notification delivery preferences channels.";
    usf:portForContract sc:notificationdeliveryandpreferencesandchannels;
    usf:permitsProviderMode <urn:usf:providermode:deterministictestsubstitute>, <urn:usf:providermode:repositorylocalservice>, <urn:usf:providermode:externalsandbox>, <urn:usf:providermode:liveexternal>;
    usf:enforcesTenantBoundary <urn:usf:tenantboundary:tenant>;
    usf:providerMaximumAttempts 3;
    usf:providerFailureBehaviour "Validate requests and responses, apply a bounded timeout, retry only classified transient failures, preserve idempotency, open a bounded circuit after repeated failure, and return a stable redacted dependency error.";
    usf:providerSecurityBoundary "Credentials are secret references scoped to this provider and environment; tenant context is explicit, outbound destinations are allow-listed, and request and response audit metadata exclude payload secrets.".
port:objectstore usf:repositoryName "object-store";
    usf:definition "Provider-independent boundary for object store, owned by object storage tenant prefixes signed urls.";
    usf:portForContract sc:objectstorageandtenantprefixesandsignedurls;
    usf:permitsProviderMode <urn:usf:providermode:deterministictestsubstitute>, <urn:usf:providermode:repositorylocalservice>, <urn:usf:providermode:externalsandbox>, <urn:usf:providermode:liveexternal>;
    usf:enforcesTenantBoundary <urn:usf:tenantboundary:tenant>;
    usf:providerMaximumAttempts 3;
    usf:providerFailureBehaviour "Validate requests and responses, apply a bounded timeout, retry only classified transient failures, preserve idempotency, open a bounded circuit after repeated failure, and return a stable redacted dependency error.";
    usf:providerSecurityBoundary "Credentials are secret references scoped to this provider and environment; tenant context is explicit, outbound destinations are allow-listed, and request and response audit metadata exclude payload secrets.".
port:observabilitysink usf:repositoryName "observability-sink";
    usf:definition "Provider-independent boundary for observability sink, owned by metrics traces.";
    usf:portForContract sc:metricsandtraces;
    usf:permitsProviderMode <urn:usf:providermode:deterministictestsubstitute>, <urn:usf:providermode:repositorylocalservice>, <urn:usf:providermode:externalsandbox>, <urn:usf:providermode:liveexternal>;
    usf:enforcesTenantBoundary <urn:usf:tenantboundary:tenant>;
    usf:providerMaximumAttempts 3;
    usf:providerFailureBehaviour "Validate requests and responses, apply a bounded timeout, retry only classified transient failures, preserve idempotency, open a bounded circuit after repeated failure, and return a stable redacted dependency error.";
    usf:providerSecurityBoundary "Credentials are secret references scoped to this provider and environment; tenant context is explicit, outbound destinations are allow-listed, and request and response audit metadata exclude payload secrets.".
port:operationaljobport usf:repositoryName "operational-job";
    usf:definition "Provider-independent boundary for operational job, owned by background workers job runner.";
    usf:portForContract sc:backgroundworkersjobrunner;
    usf:permitsProviderMode <urn:usf:providermode:deterministictestsubstitute>, <urn:usf:providermode:repositorylocalservice>;
    usf:enforcesTenantBoundary <urn:usf:tenantboundary:tenant>;
    usf:providerMaximumAttempts 3;
    usf:providerFailureBehaviour "Validate requests and responses, apply a bounded timeout, retry only classified transient failures, preserve idempotency, open a bounded circuit after repeated failure, and return a stable redacted dependency error.";
    usf:providerSecurityBoundary "Credentials are secret references scoped to this provider and environment; tenant context is explicit, outbound destinations are allow-listed, and request and response audit metadata exclude payload secrets.".
port:policydecisionpoint usf:repositoryName "policy-decision-point";
    usf:definition "Provider-independent boundary for policy decision point, owned by attribute based access control policy decision point.";
    usf:portForContract sc:abacpolicydecisionpoint;
    usf:permitsProviderMode <urn:usf:providermode:deterministictestsubstitute>, <urn:usf:providermode:repositorylocalservice>;
    usf:enforcesTenantBoundary <urn:usf:tenantboundary:tenant>;
    usf:providerMaximumAttempts 3;
    usf:providerFailureBehaviour "Validate requests and responses, apply a bounded timeout, retry only classified transient failures, preserve idempotency, open a bounded circuit after repeated failure, and return a stable redacted dependency error.";
    usf:providerSecurityBoundary "Credentials are secret references scoped to this provider and environment; tenant context is explicit, outbound destinations are allow-listed, and request and response audit metadata exclude payload secrets.".
port:qualitygateprovider usf:repositoryName "quality-gate-provider";
    usf:definition "Provider-independent boundary for quality gate provider, owned by code quality secret dependency scanning.";
    usf:portForContract sc:codequalityandsecretanddependencyscanning;
    usf:permitsProviderMode <urn:usf:providermode:deterministictestsubstitute>, <urn:usf:providermode:repositorylocalservice>, <urn:usf:providermode:externalsandbox>, <urn:usf:providermode:liveexternal>;
    usf:enforcesTenantBoundary <urn:usf:tenantboundary:platform>;
    usf:providerMaximumAttempts 3;
    usf:providerFailureBehaviour "Validate requests and responses, apply a bounded timeout, retry only classified transient failures, preserve idempotency, open a bounded circuit after repeated failure, and return a stable redacted dependency error.";
    usf:providerSecurityBoundary "Credentials are secret references scoped to this provider and environment; tenant context is explicit, outbound destinations are allow-listed, and request and response audit metadata exclude payload secrets.".
port:resourcelifecycleport usf:repositoryName "resource-lifecycle";
    usf:definition "Provider-independent boundary for resource lifecycle, owned by tenant lifecycle provision suspend delete export.";
    usf:portForContract sc:tenantlifecycleprovisionsuspenddeleteexport;
    usf:permitsProviderMode <urn:usf:providermode:deterministictestsubstitute>, <urn:usf:providermode:repositorylocalservice>;
    usf:enforcesTenantBoundary <urn:usf:tenantboundary:tenant>;
    usf:providerMaximumAttempts 3;
    usf:providerFailureBehaviour "Validate requests and responses, apply a bounded timeout, retry only classified transient failures, preserve idempotency, open a bounded circuit after repeated failure, and return a stable redacted dependency error.";
    usf:providerSecurityBoundary "Credentials are secret references scoped to this provider and environment; tenant context is explicit, outbound destinations are allow-listed, and request and response audit metadata exclude payload secrets.".
port:scanprovider usf:repositoryName "scan-provider";
    usf:definition "Provider-independent boundary for scan provider, owned by code quality secret dependency scanning.";
    usf:portForContract sc:codequalityandsecretanddependencyscanning;
    usf:permitsProviderMode <urn:usf:providermode:deterministictestsubstitute>, <urn:usf:providermode:repositorylocalservice>, <urn:usf:providermode:externalsandbox>, <urn:usf:providermode:liveexternal>;
    usf:enforcesTenantBoundary <urn:usf:tenantboundary:platform>;
    usf:providerMaximumAttempts 3;
    usf:providerFailureBehaviour "Validate requests and responses, apply a bounded timeout, retry only classified transient failures, preserve idempotency, open a bounded circuit after repeated failure, and return a stable redacted dependency error.";
    usf:providerSecurityBoundary "Credentials are secret references scoped to this provider and environment; tenant context is explicit, outbound destinations are allow-listed, and request and response audit metadata exclude payload secrets.".
port:searchindexport usf:repositoryName "search-index";
    usf:definition "Provider-independent boundary for search index, owned by search indexing product search.";
    usf:portForContract sc:searchandindexingproductsearch;
    usf:permitsProviderMode <urn:usf:providermode:deterministictestsubstitute>, <urn:usf:providermode:repositorylocalservice>, <urn:usf:providermode:externalsandbox>, <urn:usf:providermode:liveexternal>;
    usf:enforcesTenantBoundary <urn:usf:tenantboundary:tenant>;
    usf:providerMaximumAttempts 3;
    usf:providerFailureBehaviour "Validate requests and responses, apply a bounded timeout, retry only classified transient failures, preserve idempotency, open a bounded circuit after repeated failure, and return a stable redacted dependency error.";
    usf:providerSecurityBoundary "Credentials are secret references scoped to this provider and environment; tenant context is explicit, outbound destinations are allow-listed, and request and response audit metadata exclude payload secrets.".
port:secretresolver usf:repositoryName "secret-resolver";
    usf:definition "Provider-independent boundary for secret resolver, owned by runtime secrets management.";
    usf:portForContract sc:runtimesecretsmanagement;
    usf:permitsProviderMode <urn:usf:providermode:deterministictestsubstitute>, <urn:usf:providermode:repositorylocalservice>;
    usf:enforcesTenantBoundary <urn:usf:tenantboundary:tenant>;
    usf:providerMaximumAttempts 3;
    usf:providerFailureBehaviour "Validate requests and responses, apply a bounded timeout, retry only classified transient failures, preserve idempotency, open a bounded circuit after repeated failure, and return a stable redacted dependency error.";
    usf:providerSecurityBoundary "Credentials are secret references scoped to this provider and environment; tenant context is explicit, outbound destinations are allow-listed, and request and response audit metadata exclude payload secrets.".
port:secretstore usf:repositoryName "secret-store";
    usf:definition "Provider-independent boundary for secret store, owned by write only secret settings.";
    usf:portForContract sc:writeonlysecretsettings;
    usf:permitsProviderMode <urn:usf:providermode:deterministictestsubstitute>, <urn:usf:providermode:repositorylocalservice>;
    usf:enforcesTenantBoundary <urn:usf:tenantboundary:tenant>;
    usf:providerMaximumAttempts 3;
    usf:providerFailureBehaviour "Validate requests and responses, apply a bounded timeout, retry only classified transient failures, preserve idempotency, open a bounded circuit after repeated failure, and return a stable redacted dependency error.";
    usf:providerSecurityBoundary "Credentials are secret references scoped to this provider and environment; tenant context is explicit, outbound destinations are allow-listed, and request and response audit metadata exclude payload secrets.".
port:sessionstore usf:repositoryName "session-store";
    usf:definition "Provider-independent boundary for session store, owned by authentication platform.";
    usf:portForContract sc:authenticationplatform;
    usf:permitsProviderMode <urn:usf:providermode:deterministictestsubstitute>, <urn:usf:providermode:repositorylocalservice>;
    usf:enforcesTenantBoundary <urn:usf:tenantboundary:tenant>;
    usf:providerMaximumAttempts 3;
    usf:providerFailureBehaviour "Validate requests and responses, apply a bounded timeout, retry only classified transient failures, preserve idempotency, open a bounded circuit after repeated failure, and return a stable redacted dependency error.";
    usf:providerSecurityBoundary "Credentials are secret references scoped to this provider and environment; tenant context is explicit, outbound destinations are allow-listed, and request and response audit metadata exclude payload secrets.".
port:siemforwarder usf:repositoryName "security-event-forwarder";
    usf:definition "Provider-independent boundary for security event forwarder, owned by audit of privileged access.";
    usf:portForContract sc:auditofprivilegedaccess;
    usf:permitsProviderMode <urn:usf:providermode:deterministictestsubstitute>, <urn:usf:providermode:repositorylocalservice>, <urn:usf:providermode:externalsandbox>, <urn:usf:providermode:liveexternal>;
    usf:enforcesTenantBoundary <urn:usf:tenantboundary:platform>;
    usf:providerMaximumAttempts 3;
    usf:providerFailureBehaviour "Validate requests and responses, apply a bounded timeout, retry only classified transient failures, preserve idempotency, open a bounded circuit after repeated failure, and return a stable redacted dependency error.";
    usf:providerSecurityBoundary "Credentials are secret references scoped to this provider and environment; tenant context is explicit, outbound destinations are allow-listed, and request and response audit metadata exclude payload secrets.".
port:signedurlissuer usf:repositoryName "signed-url-issuer";
    usf:definition "Provider-independent boundary for signed url issuer, owned by object storage tenant prefixes signed urls.";
    usf:portForContract sc:objectstorageandtenantprefixesandsignedurls;
    usf:permitsProviderMode <urn:usf:providermode:deterministictestsubstitute>, <urn:usf:providermode:repositorylocalservice>;
    usf:enforcesTenantBoundary <urn:usf:tenantboundary:tenant>;
    usf:providerMaximumAttempts 3;
    usf:providerFailureBehaviour "Validate requests and responses, apply a bounded timeout, retry only classified transient failures, preserve idempotency, open a bounded circuit after repeated failure, and return a stable redacted dependency error.";
    usf:providerSecurityBoundary "Credentials are secret references scoped to this provider and environment; tenant context is explicit, outbound destinations are allow-listed, and request and response audit metadata exclude payload secrets.".
port:telemetryport usf:repositoryName "telemetry";
    usf:definition "Provider-independent boundary for telemetry, owned by metrics traces.";
    usf:portForContract sc:metricsandtraces;
    usf:permitsProviderMode <urn:usf:providermode:deterministictestsubstitute>, <urn:usf:providermode:repositorylocalservice>;
    usf:enforcesTenantBoundary <urn:usf:tenantboundary:tenant>;
    usf:providerMaximumAttempts 3;
    usf:providerFailureBehaviour "Validate requests and responses, apply a bounded timeout, retry only classified transient failures, preserve idempotency, open a bounded circuit after repeated failure, and return a stable redacted dependency error.";
    usf:providerSecurityBoundary "Credentials are secret references scoped to this provider and environment; tenant context is explicit, outbound destinations are allow-listed, and request and response audit metadata exclude payload secrets.".
port:tenantmembershipdirectory usf:repositoryName "tenant-membership-directory";
    usf:definition "Provider-independent boundary for tenant membership directory, owned by user identity tenant membership.";
    usf:portForContract sc:useridentityandtenantmembership;
    usf:permitsProviderMode <urn:usf:providermode:deterministictestsubstitute>, <urn:usf:providermode:repositorylocalservice>;
    usf:enforcesTenantBoundary <urn:usf:tenantboundary:tenant>;
    usf:providerMaximumAttempts 3;
    usf:providerFailureBehaviour "Validate requests and responses, apply a bounded timeout, retry only classified transient failures, preserve idempotency, open a bounded circuit after repeated failure, and return a stable redacted dependency error.";
    usf:providerSecurityBoundary "Credentials are secret references scoped to this provider and environment; tenant context is explicit, outbound destinations are allow-listed, and request and response audit metadata exclude payload secrets.".
port:tenantscopedrepository usf:repositoryName "tenant-scoped-repository";
    usf:definition "Provider-independent boundary for tenant scoped repository, owned by relational storage schema changes row level security.";
    usf:portForContract sc:relationalstorageandmigrationsandrls;
    usf:permitsProviderMode <urn:usf:providermode:deterministictestsubstitute>, <urn:usf:providermode:repositorylocalservice>;
    usf:enforcesTenantBoundary <urn:usf:tenantboundary:tenant>;
    usf:providerMaximumAttempts 3;
    usf:providerFailureBehaviour "Validate requests and responses, apply a bounded timeout, retry only classified transient failures, preserve idempotency, open a bounded circuit after repeated failure, and return a stable redacted dependency error.";
    usf:providerSecurityBoundary "Credentials are secret references scoped to this provider and environment; tenant context is explicit, outbound destinations are allow-listed, and request and response audit metadata exclude payload secrets.".
port:webhookcaptureprovider usf:repositoryName "webhook-capture-provider";
    usf:definition "Provider-independent boundary for webhook capture provider, owned by developer webhooks.";
    usf:portForContract sc:webhooksdeveloperfacing;
    usf:permitsProviderMode <urn:usf:providermode:deterministictestsubstitute>, <urn:usf:providermode:repositorylocalservice>, <urn:usf:providermode:externalsandbox>, <urn:usf:providermode:liveexternal>;
    usf:enforcesTenantBoundary <urn:usf:tenantboundary:tenant>;
    usf:providerMaximumAttempts 3;
    usf:providerFailureBehaviour "Validate requests and responses, apply a bounded timeout, retry only classified transient failures, preserve idempotency, open a bounded circuit after repeated failure, and return a stable redacted dependency error.";
    usf:providerSecurityBoundary "Credentials are secret references scoped to this provider and environment; tenant context is explicit, outbound destinations are allow-listed, and request and response audit metadata exclude payload secrets.".
port:workflowengine usf:repositoryName "workflow-engine";
    usf:definition "Provider-independent boundary for workflow engine, owned by workflow engine scheduled jobs approvals.";
    usf:portForContract sc:workflowenginescheduledjobsapprovals;
    usf:permitsProviderMode <urn:usf:providermode:deterministictestsubstitute>, <urn:usf:providermode:repositorylocalservice>;
    usf:enforcesTenantBoundary <urn:usf:tenantboundary:tenant>;
    usf:providerMaximumAttempts 3;
    usf:providerFailureBehaviour "Validate requests and responses, apply a bounded timeout, retry only classified transient failures, preserve idempotency, open a bounded circuit after repeated failure, and return a stable redacted dependency error.";
    usf:providerSecurityBoundary "Credentials are secret references scoped to this provider and environment; tenant context is explicit, outbound destinations are allow-listed, and request and response audit metadata exclude payload secrets."
}
